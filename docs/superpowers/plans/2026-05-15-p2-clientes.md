# P2 — Clientes (Wenox PWA CRM) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cadastro de clientes 100% usável no mobile: listar com busca/filtros, criar, editar e ver o detalhe (aba Info), sobre a fundação do P1.

**Architecture:** Coleção `clientes` no PocketBase com regras de acesso por autenticação. Camada de serviço (`src/clientes/clientesService.ts`) isola o SDK; páginas Ionic consomem o serviço. Navegação por bottom tabs. Estado de lista local com refetch após mutações.

**Tech Stack:** Ionic React 8, PocketBase SDK, react-router v5 (já no projeto), Vitest + testing-library.

**Pré-requisitos (do P1, já prontos):** `pb` singleton em `src/lib/pocketbase.ts`; `useAuth`/`AuthProvider`; alias `@`; deploy automático no push da `main`.

---

## File Structure

```
src/
├── clientes/
│   ├── types.ts                 # tipo Cliente + constantes (status, categoria, servicos)
│   ├── clientesService.ts       # CRUD via pb.collection('clientes')
│   ├── ClientesListPage.tsx     # lista + busca + filtros + FAB
│   ├── ClienteFormPage.tsx      # criar/editar
│   └── ClienteDetailPage.tsx    # detalhe (aba Info) + ações de contato
├── components/
│   └── AppTabs.tsx              # bottom tab bar (Clientes / Config stub)
├── pages/HomePage.tsx           # (substituída pelo roteamento de tabs)
└── App.tsx                      # (modificar rotas)
tests/
├── clientesService.test.ts
├── ClientesListPage.test.tsx
└── ClienteFormPage.test.tsx
```

---

## Task 1: Criar a coleção `clientes` no PocketBase

**Files:** nenhum no repo — backend via API de superadmin.

- [ ] **Step 1: Autenticar superadmin**

Run:
```bash
TOKEN=$(curl -s -X POST https://api.wenox.com.br/api/collections/_superusers/auth-with-password -H "Content-Type: application/json" -d '{"identity":"adm@wenox.com.br","password":"22jGwRjw1sbXcSqexWKh"}' | python3 -c 'import sys,json;print(json.load(sys.stdin)["token"])') && echo "LEN=${#TOKEN}"
```
Expected: `LEN` > 100.

- [ ] **Step 2: Criar a coleção `clientes`**

Run:
```bash
curl -s -X POST https://api.wenox.com.br/api/collections -H "Authorization: $TOKEN" -H "Content-Type: application/json" -d '{
  "name":"clientes","type":"base",
  "listRule":"@request.auth.id != \"\"",
  "viewRule":"@request.auth.id != \"\"",
  "createRule":"@request.auth.id != \"\"",
  "updateRule":"@request.auth.id != \"\"",
  "deleteRule":"@request.auth.id != \"\"",
  "fields":[
    {"name":"nome_fantasia","type":"text","required":true},
    {"name":"razao_social","type":"text"},
    {"name":"cnpj","type":"text"},
    {"name":"categoria","type":"select","required":true,"maxSelect":1,"values":["Cliente","Parceiro"]},
    {"name":"origem","type":"select","maxSelect":1,"values":["Indicacao","Site","Trafego","Outros"]},
    {"name":"servicos","type":"select","maxSelect":5,"values":["Social Media","Trafego Pago","Desenvolvimento","Branding","Outros"]},
    {"name":"telefone","type":"text","required":true},
    {"name":"email","type":"email"},
    {"name":"site","type":"url"},
    {"name":"status","type":"select","required":true,"maxSelect":1,"values":["Ativo","Inativo"]},
    {"name":"data_inicio","type":"date"},
    {"name":"data_encerramento","type":"date"},
    {"name":"observacoes","type":"editor"},
    {"name":"logo","type":"file","maxSelect":1,"mimeTypes":["image/png","image/jpeg","image/webp"]}
  ]
}' -w "\nHTTP %{http_code}\n" | tail -2
```
Expected: `HTTP 200`.

- [ ] **Step 3: Verificar criação autenticado como usuário**

Run:
```bash
UTOKEN=$(curl -s -X POST https://api.wenox.com.br/api/collections/usuarios/auth-with-password -H "Content-Type: application/json" -d '{"identity":"leonardo@wenox.com.br","password":"TrocarNoPrimeiroLogin#2026"}' | python3 -c 'import sys,json;print(json.load(sys.stdin)["token"])') && curl -s -X POST https://api.wenox.com.br/api/collections/clientes/records -H "Authorization: $UTOKEN" -H "Content-Type: application/json" -d '{"nome_fantasia":"Cliente Teste","categoria":"Cliente","telefone":"11999999999","status":"Ativo"}' -w "\nHTTP %{http_code}\n" | tail -1
```
Expected: `HTTP 200`.

- [ ] **Step 4: Limpar o registro de teste**

Run:
```bash
ID=$(curl -s "https://api.wenox.com.br/api/collections/clientes/records?filter=(nome_fantasia='Cliente Teste')" -H "Authorization: $UTOKEN" | python3 -c 'import sys,json;print(json.load(sys.stdin)["items"][0]["id"])') && curl -s -X DELETE "https://api.wenox.com.br/api/collections/clientes/records/$ID" -H "Authorization: $UTOKEN" -w "del HTTP %{http_code}\n"
```
Expected: `del HTTP 204`.

> Sem commit (backend).

---

## Task 2: Tipos + serviço de clientes — TDD

**Files:**
- Create: `src/clientes/types.ts`, `src/clientes/clientesService.ts`
- Test: `tests/clientesService.test.ts`

- [ ] **Step 1: Criar `src/clientes/types.ts`**

```ts
export const CATEGORIAS = ['Cliente', 'Parceiro'] as const;
export const STATUS = ['Ativo', 'Inativo'] as const;
export const ORIGENS = ['Indicacao', 'Site', 'Trafego', 'Outros'] as const;
export const SERVICOS = [
  'Social Media', 'Trafego Pago', 'Desenvolvimento', 'Branding', 'Outros',
] as const;

export interface Cliente {
  id: string;
  nome_fantasia: string;
  razao_social?: string;
  cnpj?: string;
  categoria: (typeof CATEGORIAS)[number];
  origem?: (typeof ORIGENS)[number];
  servicos?: string[];
  telefone: string;
  email?: string;
  site?: string;
  status: (typeof STATUS)[number];
  data_inicio?: string;
  data_encerramento?: string;
  observacoes?: string;
}

export type ClienteInput = Omit<Cliente, 'id'>;
```

- [ ] **Step 2: Escrever o teste que falha — `tests/clientesService.test.ts`**

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';

const getList = vi.fn();
const getOne = vi.fn();
const create = vi.fn();
const update = vi.fn();
const del = vi.fn();

vi.mock('@/lib/pocketbase', () => ({
  pb: {
    collection: () => ({
      getList,
      getOne,
      create,
      update,
      delete: del,
    }),
  },
}));

import { listClientes, getCliente, createCliente, updateCliente, deleteCliente } from '@/clientes/clientesService';

describe('clientesService', () => {
  beforeEach(() => vi.clearAllMocks());

  it('listClientes repassa busca como filtro e retorna items', async () => {
    getList.mockResolvedValue({ items: [{ id: '1', nome_fantasia: 'ACME' }] });
    const r = await listClientes('acme');
    expect(getList).toHaveBeenCalledWith(1, 100, expect.objectContaining({
      filter: expect.stringContaining('acme'),
      sort: '-created',
    }));
    expect(r).toEqual([{ id: '1', nome_fantasia: 'ACME' }]);
  });

  it('listClientes sem busca não envia filtro de texto', async () => {
    getList.mockResolvedValue({ items: [] });
    await listClientes('');
    expect(getList).toHaveBeenCalledWith(1, 100, expect.objectContaining({ sort: '-created' }));
  });

  it('getCliente delega para getOne', async () => {
    getOne.mockResolvedValue({ id: '9' });
    expect(await getCliente('9')).toEqual({ id: '9' });
    expect(getOne).toHaveBeenCalledWith('9');
  });

  it('createCliente delega para create', async () => {
    create.mockResolvedValue({ id: 'new' });
    const r = await createCliente({ nome_fantasia: 'X', categoria: 'Cliente', telefone: '1', status: 'Ativo' } as any);
    expect(create).toHaveBeenCalled();
    expect(r).toEqual({ id: 'new' });
  });

  it('updateCliente delega para update', async () => {
    update.mockResolvedValue({ id: '1' });
    await updateCliente('1', { nome_fantasia: 'Y' } as any);
    expect(update).toHaveBeenCalledWith('1', { nome_fantasia: 'Y' });
  });

  it('deleteCliente delega para delete', async () => {
    del.mockResolvedValue(true);
    await deleteCliente('1');
    expect(del).toHaveBeenCalledWith('1');
  });
});
```

- [ ] **Step 3: Rodar e confirmar que falha**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npx vitest run tests/clientesService.test.ts 2>&1 | grep -E "FAIL|Cannot find"
```
Expected: FAIL — `@/clientes/clientesService` não encontrado.

- [ ] **Step 4: Implementar `src/clientes/clientesService.ts`**

```ts
import { pb } from '@/lib/pocketbase';
import type { Cliente, ClienteInput } from './types';

const col = () => pb.collection('clientes');

export async function listClientes(busca: string): Promise<Cliente[]> {
  const opts: Record<string, unknown> = { sort: '-created' };
  const q = busca.trim();
  if (q) {
    const safe = q.replace(/"/g, '');
    opts.filter = `nome_fantasia ~ "${safe}" || razao_social ~ "${safe}"`;
  }
  const res = await col().getList(1, 100, opts);
  return res.items as unknown as Cliente[];
}

export async function getCliente(id: string): Promise<Cliente> {
  return (await col().getOne(id)) as unknown as Cliente;
}

export async function createCliente(input: ClienteInput): Promise<Cliente> {
  return (await col().create(input)) as unknown as Cliente;
}

export async function updateCliente(
  id: string,
  input: Partial<ClienteInput>
): Promise<Cliente> {
  return (await col().update(id, input)) as unknown as Cliente;
}

export async function deleteCliente(id: string): Promise<void> {
  await col().delete(id);
}
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npx vitest run tests/clientesService.test.ts 2>&1 | grep -E "Test Files|Tests "
```
Expected: PASS (6 testes).

- [ ] **Step 6: Commit**

```bash
git add src/clientes/types.ts src/clientes/clientesService.ts tests/clientesService.test.ts && git commit -m "feat: tipos e servico de clientes"
```

---

## Task 3: Lista de Clientes (busca + filtros + FAB) — TDD

**Files:**
- Create: `src/clientes/ClientesListPage.tsx`
- Test: `tests/ClientesListPage.test.tsx`

- [ ] **Step 1: Escrever o teste que falha — `tests/ClientesListPage.test.tsx`**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';

const listClientes = vi.fn();
vi.mock('@/clientes/clientesService', () => ({ listClientes }));
vi.mock('react-router-dom', () => ({ useHistory: () => ({ push: vi.fn() }) }));

import { ClientesListPage } from '@/clientes/ClientesListPage';

describe('ClientesListPage', () => {
  beforeEach(() => vi.clearAllMocks());

  it('lista clientes ao montar', async () => {
    listClientes.mockResolvedValue([
      { id: '1', nome_fantasia: 'ACME', status: 'Ativo', telefone: '11', categoria: 'Cliente' },
    ]);
    render(<ClientesListPage />);
    expect(await screen.findByText('ACME')).toBeInTheDocument();
    expect(listClientes).toHaveBeenCalledWith('');
  });

  it('refaz a busca ao digitar', async () => {
    listClientes.mockResolvedValue([]);
    render(<ClientesListPage />);
    await waitFor(() => expect(listClientes).toHaveBeenCalled());
    await userEvent.type(screen.getByPlaceholderText(/buscar/i), 'acme');
    await waitFor(() =>
      expect(listClientes).toHaveBeenLastCalledWith('acme')
    );
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npx vitest run tests/ClientesListPage.test.tsx 2>&1 | grep -E "FAIL|Cannot find"
```
Expected: FAIL — módulo não encontrado.

- [ ] **Step 3: Implementar `src/clientes/ClientesListPage.tsx`**

```tsx
import { useEffect, useState, useCallback } from 'react';
import {
  IonPage, IonHeader, IonToolbar, IonTitle, IonContent, IonSearchbar,
  IonList, IonItem, IonLabel, IonBadge, IonFab, IonFabButton, IonIcon,
  IonChip, IonRefresher, IonRefresherContent,
} from '@ionic/react';
import { add } from 'ionicons/icons';
import { useHistory } from 'react-router-dom';
import { listClientes } from '@/clientes/clientesService';
import type { Cliente } from '@/clientes/types';

export function ClientesListPage() {
  const history = useHistory();
  const [busca, setBusca] = useState('');
  const [filtro, setFiltro] = useState<'Todos' | 'Ativo' | 'Inativo'>('Todos');
  const [clientes, setClientes] = useState<Cliente[]>([]);

  const carregar = useCallback(async (q: string) => {
    setClientes(await listClientes(q));
  }, []);

  useEffect(() => {
    carregar(busca);
  }, [busca, carregar]);

  const visiveis = clientes.filter(
    (c) => filtro === 'Todos' || c.status === filtro
  );

  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonTitle>Clientes</IonTitle>
        </IonToolbar>
        <IonToolbar>
          <IonSearchbar
            placeholder="Buscar"
            value={busca}
            onIonInput={(e) => setBusca(e.detail.value ?? '')}
          />
        </IonToolbar>
      </IonHeader>
      <IonContent>
        <IonRefresher slot="fixed" onIonRefresh={async (e) => { await carregar(busca); e.detail.complete(); }}>
          <IonRefresherContent />
        </IonRefresher>
        <div className="ion-padding-start ion-padding-top">
          {(['Todos', 'Ativo', 'Inativo'] as const).map((f) => (
            <IonChip
              key={f}
              color={filtro === f ? 'primary' : 'medium'}
              onClick={() => setFiltro(f)}
            >
              {f}
            </IonChip>
          ))}
        </div>
        <IonList>
          {visiveis.map((c) => (
            <IonItem
              key={c.id}
              button
              onClick={() => history.push(`/clientes/${c.id}`)}
            >
              <IonLabel>
                <h2>{c.nome_fantasia}</h2>
                <p>{c.telefone}</p>
              </IonLabel>
              <IonBadge
                slot="end"
                color={c.status === 'Ativo' ? 'success' : 'medium'}
              >
                {c.status}
              </IonBadge>
            </IonItem>
          ))}
        </IonList>
        <IonFab slot="fixed" vertical="bottom" horizontal="end">
          <IonFabButton onClick={() => history.push('/clientes/novo')}>
            <IonIcon icon={add} />
          </IonFabButton>
        </IonFab>
      </IonContent>
    </IonPage>
  );
}
```

- [ ] **Step 4: Instalar ionicons (se ausente) e rodar o teste**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npm ls ionicons >/dev/null 2>&1 || npm i ionicons >/dev/null 2>&1; npx vitest run tests/ClientesListPage.test.tsx 2>&1 | grep -E "Test Files|Tests "
```
Expected: PASS (2 testes).

- [ ] **Step 5: Commit**

```bash
git add src/clientes/ClientesListPage.tsx tests/ClientesListPage.test.tsx package.json package-lock.json && git commit -m "feat: lista de clientes com busca e filtros"
```

---

## Task 4: Formulário criar/editar Cliente — TDD

**Files:**
- Create: `src/clientes/ClienteFormPage.tsx`
- Test: `tests/ClienteFormPage.test.tsx`

- [ ] **Step 1: Escrever o teste que falha — `tests/ClienteFormPage.test.tsx`**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';

const createCliente = vi.fn();
const getCliente = vi.fn();
const updateCliente = vi.fn();
const push = vi.fn();

vi.mock('@/clientes/clientesService', () => ({ createCliente, getCliente, updateCliente }));
vi.mock('react-router-dom', () => ({
  useHistory: () => ({ push }),
  useParams: () => ({}),
}));

import { ClienteFormPage } from '@/clientes/ClienteFormPage';

describe('ClienteFormPage (criar)', () => {
  beforeEach(() => vi.clearAllMocks());

  it('cria cliente e navega de volta', async () => {
    createCliente.mockResolvedValue({ id: 'novo' });
    render(<ClienteFormPage />);
    await userEvent.type(screen.getByLabelText(/nome fantasia/i), 'ACME');
    await userEvent.type(screen.getByLabelText(/telefone/i), '11988887777');
    await userEvent.click(screen.getByRole('button', { name: /salvar/i }));
    expect(createCliente).toHaveBeenCalledWith(
      expect.objectContaining({ nome_fantasia: 'ACME', telefone: '11988887777' })
    );
    expect(push).toHaveBeenCalledWith('/clientes');
  });

  it('exige nome fantasia', async () => {
    render(<ClienteFormPage />);
    await userEvent.click(screen.getByRole('button', { name: /salvar/i }));
    expect(createCliente).not.toHaveBeenCalled();
    expect(screen.getByText(/nome fantasia é obrigatório/i)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npx vitest run tests/ClienteFormPage.test.tsx 2>&1 | grep -E "FAIL|Cannot find"
```
Expected: FAIL — módulo não encontrado.

- [ ] **Step 3: Implementar `src/clientes/ClienteFormPage.tsx`**

```tsx
import { useEffect, useState } from 'react';
import {
  IonPage, IonHeader, IonToolbar, IonTitle, IonContent, IonButtons,
  IonBackButton,
} from '@ionic/react';
import { useHistory, useParams } from 'react-router-dom';
import {
  createCliente, getCliente, updateCliente,
} from '@/clientes/clientesService';
import { CATEGORIAS, STATUS } from '@/clientes/types';
import type { ClienteInput } from '@/clientes/types';

const vazio: ClienteInput = {
  nome_fantasia: '', categoria: 'Cliente', telefone: '', status: 'Ativo',
};

export function ClienteFormPage() {
  const history = useHistory();
  const { id } = useParams<{ id?: string }>();
  const [form, setForm] = useState<ClienteInput>(vazio);
  const [erro, setErro] = useState('');

  useEffect(() => {
    if (id) {
      getCliente(id).then((c) => {
        const { id: _omit, ...rest } = c;
        void _omit;
        setForm(rest as ClienteInput);
      });
    }
  }, [id]);

  function set<K extends keyof ClienteInput>(k: K, v: ClienteInput[K]) {
    setForm((f) => ({ ...f, [k]: v }));
  }

  async function salvar(e: React.FormEvent) {
    e.preventDefault();
    if (!form.nome_fantasia.trim()) {
      setErro('Nome fantasia é obrigatório');
      return;
    }
    if (id) await updateCliente(id, form);
    else await createCliente(form);
    history.push('/clientes');
  }

  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonButtons slot="start">
            <IonBackButton defaultHref="/clientes" />
          </IonButtons>
          <IonTitle>{id ? 'Editar' : 'Novo'} Cliente</IonTitle>
        </IonToolbar>
      </IonHeader>
      <IonContent className="ion-padding">
        <form onSubmit={salvar}>
          <label htmlFor="nf">Nome fantasia</label>
          <input
            id="nf"
            value={form.nome_fantasia}
            onChange={(e) => set('nome_fantasia', e.target.value)}
            style={{ width: '100%', padding: 12, marginBottom: 12 }}
          />
          <label htmlFor="tel">Telefone</label>
          <input
            id="tel"
            value={form.telefone}
            onChange={(e) => set('telefone', e.target.value)}
            style={{ width: '100%', padding: 12, marginBottom: 12 }}
          />
          <label htmlFor="cat">Categoria</label>
          <select
            id="cat"
            value={form.categoria}
            onChange={(e) => set('categoria', e.target.value as ClienteInput['categoria'])}
            style={{ width: '100%', padding: 12, marginBottom: 12 }}
          >
            {CATEGORIAS.map((c) => <option key={c} value={c}>{c}</option>)}
          </select>
          <label htmlFor="st">Status</label>
          <select
            id="st"
            value={form.status}
            onChange={(e) => set('status', e.target.value as ClienteInput['status'])}
            style={{ width: '100%', padding: 12, marginBottom: 12 }}
          >
            {STATUS.map((s) => <option key={s} value={s}>{s}</option>)}
          </select>
          {erro && <p style={{ color: 'var(--ion-color-danger, #eb445a)' }}>{erro}</p>}
          <button
            type="submit"
            style={{
              width: '100%', padding: 14, marginTop: 12,
              background: 'var(--ion-color-primary)',
              color: 'var(--ion-color-primary-contrast)',
              border: 'none', borderRadius: 8, fontSize: 16,
            }}
          >
            Salvar
          </button>
        </form>
      </IonContent>
    </IonPage>
  );
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npx vitest run tests/ClienteFormPage.test.tsx 2>&1 | grep -E "Test Files|Tests "
```
Expected: PASS (2 testes).

- [ ] **Step 5: Commit**

```bash
git add src/clientes/ClienteFormPage.tsx tests/ClienteFormPage.test.tsx && git commit -m "feat: formulario criar/editar cliente"
```

---

## Task 5: Detalhe do Cliente (aba Info) + ações de contato

**Files:**
- Create: `src/clientes/ClienteDetailPage.tsx`
- Test: `tests/ClienteDetailPage.test.tsx`

- [ ] **Step 1: Escrever o teste que falha — `tests/ClienteDetailPage.test.tsx`**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';

const getCliente = vi.fn();
vi.mock('@/clientes/clientesService', () => ({ getCliente }));
vi.mock('react-router-dom', () => ({
  useParams: () => ({ id: 'c1' }),
  useHistory: () => ({ push: vi.fn() }),
}));

import { ClienteDetailPage } from '@/clientes/ClienteDetailPage';

describe('ClienteDetailPage', () => {
  beforeEach(() => vi.clearAllMocks());

  it('mostra dados e link de WhatsApp', async () => {
    getCliente.mockResolvedValue({
      id: 'c1', nome_fantasia: 'ACME', telefone: '11988887777',
      categoria: 'Cliente', status: 'Ativo', email: 'a@a.com',
    });
    render(<ClienteDetailPage />);
    expect(await screen.findByText('ACME')).toBeInTheDocument();
    const wpp = screen.getByRole('link', { name: /whatsapp/i });
    expect(wpp).toHaveAttribute('href', expect.stringContaining('wa.me/11988887777'));
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npx vitest run tests/ClienteDetailPage.test.tsx 2>&1 | grep -E "FAIL|Cannot find"
```
Expected: FAIL — módulo não encontrado.

- [ ] **Step 3: Implementar `src/clientes/ClienteDetailPage.tsx`**

```tsx
import { useEffect, useState } from 'react';
import {
  IonPage, IonHeader, IonToolbar, IonTitle, IonContent, IonButtons,
  IonBackButton, IonButton, IonBadge, IonList, IonItem, IonLabel,
} from '@ionic/react';
import { useParams, useHistory } from 'react-router-dom';
import { getCliente } from '@/clientes/clientesService';
import type { Cliente } from '@/clientes/types';

export function ClienteDetailPage() {
  const { id } = useParams<{ id: string }>();
  const history = useHistory();
  const [c, setC] = useState<Cliente | null>(null);

  useEffect(() => {
    getCliente(id).then(setC);
  }, [id]);

  if (!c) {
    return (
      <IonPage>
        <IonContent className="ion-padding">Carregando…</IonContent>
      </IonPage>
    );
  }

  const wpp = `https://wa.me/${c.telefone.replace(/\D/g, '')}`;

  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonButtons slot="start">
            <IonBackButton defaultHref="/clientes" />
          </IonButtons>
          <IonTitle>{c.nome_fantasia}</IonTitle>
          <IonButtons slot="end">
            <IonButton onClick={() => history.push(`/clientes/${c.id}/editar`)}>
              Editar
            </IonButton>
          </IonButtons>
        </IonToolbar>
      </IonHeader>
      <IonContent className="ion-padding">
        <h2>
          {c.nome_fantasia}{' '}
          <IonBadge color={c.status === 'Ativo' ? 'success' : 'medium'}>
            {c.status}
          </IonBadge>
        </h2>
        <IonList>
          <IonItem>
            <IonLabel>
              <p>Categoria</p>
              <h3>{c.categoria}</h3>
            </IonLabel>
          </IonItem>
          <IonItem>
            <IonLabel>
              <p>Telefone</p>
              <h3>{c.telefone}</h3>
            </IonLabel>
          </IonItem>
          {c.email && (
            <IonItem>
              <IonLabel>
                <p>E-mail</p>
                <h3>{c.email}</h3>
              </IonLabel>
            </IonItem>
          )}
        </IonList>
        <div style={{ display: 'flex', gap: 8, marginTop: 16 }}>
          <a
            href={wpp}
            target="_blank"
            rel="noopener"
            aria-label="WhatsApp"
            style={{
              flex: 1, textAlign: 'center', padding: 12,
              background: 'var(--ion-color-primary)',
              color: 'var(--ion-color-primary-contrast)',
              borderRadius: 8, textDecoration: 'none',
            }}
          >
            WhatsApp
          </a>
          <a
            href={`tel:${c.telefone}`}
            aria-label="Ligar"
            style={{
              flex: 1, textAlign: 'center', padding: 12,
              border: '1px solid var(--ion-color-primary)',
              color: 'var(--ion-color-primary)',
              borderRadius: 8, textDecoration: 'none',
            }}
          >
            Ligar
          </a>
        </div>
      </IonContent>
    </IonPage>
  );
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npx vitest run tests/ClienteDetailPage.test.tsx 2>&1 | grep -E "Test Files|Tests "
```
Expected: PASS (1 teste).

- [ ] **Step 5: Commit**

```bash
git add src/clientes/ClienteDetailPage.tsx tests/ClienteDetailPage.test.tsx && git commit -m "feat: detalhe do cliente + acoes de contato"
```

---

## Task 6: Navegação por tabs + rotas

**Files:**
- Create: `src/components/AppTabs.tsx`
- Modify: `src/App.tsx` (rotas autenticadas via tabs)
- Delete: `src/pages/HomePage.tsx` (substituída)

- [ ] **Step 1: Criar `src/components/AppTabs.tsx`**

```tsx
import {
  IonTabs, IonTabBar, IonTabButton, IonIcon, IonLabel,
  IonRouterOutlet,
} from '@ionic/react';
import { people, settings } from 'ionicons/icons';
import { Redirect, Route } from 'react-router-dom';
import { ClientesListPage } from '@/clientes/ClientesListPage';
import { ClienteFormPage } from '@/clientes/ClienteFormPage';
import { ClienteDetailPage } from '@/clientes/ClienteDetailPage';
import { useAuth } from '@/auth/useAuth';

function ConfigPage() {
  const { user, logout } = useAuth();
  return (
    <div className="ion-padding">
      <p>{user?.email}</p>
      <button onClick={logout}>Sair</button>
    </div>
  );
}

export function AppTabs() {
  return (
    <IonTabs>
      <IonRouterOutlet>
        <Route exact path="/clientes" component={ClientesListPage} />
        <Route exact path="/clientes/novo" component={ClienteFormPage} />
        <Route exact path="/clientes/:id" component={ClienteDetailPage} />
        <Route exact path="/clientes/:id/editar" component={ClienteFormPage} />
        <Route exact path="/config" component={ConfigPage} />
        <Route exact path="/">
          <Redirect to="/clientes" />
        </Route>
      </IonRouterOutlet>
      <IonTabBar slot="bottom">
        <IonTabButton tab="clientes" href="/clientes">
          <IonIcon icon={people} />
          <IonLabel>Clientes</IonLabel>
        </IonTabButton>
        <IonTabButton tab="config" href="/config">
          <IonIcon icon={settings} />
          <IonLabel>Config</IonLabel>
        </IonTabButton>
      </IonTabBar>
    </IonTabs>
  );
}
```

- [ ] **Step 2: Substituir o bloco autenticado em `src/App.tsx`**

Trocar a `<Route exact path="/">` que renderizava `HomePage` por um catch-all autenticado. O `App.tsx` deve ficar:

```tsx
import { Redirect, Route } from 'react-router-dom';
import { IonApp, IonRouterOutlet, setupIonicReact } from '@ionic/react';
import { IonReactRouter } from '@ionic/react-router';
import { useAuth } from '@/auth/useAuth';
import { LoginPage } from '@/pages/LoginPage';
import { AppTabs } from '@/components/AppTabs';

import '@ionic/react/css/core.css';
import '@ionic/react/css/normalize.css';
import '@ionic/react/css/structure.css';
import '@ionic/react/css/typography.css';
import './theme/variables.css';

setupIonicReact();

export default function App() {
  const { user } = useAuth();
  return (
    <IonApp>
      <IonReactRouter>
        <IonRouterOutlet>
          <Route exact path="/login">
            {user ? <Redirect to="/clientes" /> : <LoginPage />}
          </Route>
          <Route path="/">
            {user ? <AppTabs /> : <Redirect to="/login" />}
          </Route>
        </IonRouterOutlet>
      </IonReactRouter>
    </IonApp>
  );
}
```

- [ ] **Step 3: Remover `src/pages/HomePage.tsx`**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && git rm src/pages/HomePage.tsx
```

- [ ] **Step 4: Build + suíte completa**

Run:
```bash
cd "/home/leonardo-groff/projetos/Agencia Wenox" && export NVM_DIR="$HOME/.config/nvm" && . "$NVM_DIR/nvm.sh" && npm run build 2>&1 | tail -3 && npm test 2>&1 | grep -E "Test Files|Tests "
```
Expected: build sem erro; todos os testes PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/AppTabs.tsx src/App.tsx && git commit -m "feat: navegacao por tabs + rotas de clientes"
```

---

## Task 7: Deploy e smoke test

- [ ] **Step 1: Merge na main + push (gatilho de auto-deploy)**

> Executado pela skill finishing-a-development-branch após aprovação. Push na `main` dispara rebuild automático no EasyPanel.

- [ ] **Step 2: Verificar deploy**

Run:
```bash
curl -s -o /dev/null -w "app HTTP %{http_code}\n" -m 20 https://app.wenox.com.br/clientes
```
Expected: `app HTTP 200` (SPA fallback serve index).

- [ ] **Step 3: Smoke test manual**

No celular: login → aba Clientes → criar um cliente → aparece na lista → abrir detalhe → editar → WhatsApp/Ligar funcionam.

---

## Self-Review

**1. Spec coverage (P2 do spec MVP):** coleção `clientes` com todos os campos do modelo de dados ✓ (T1); serviço CRUD ✓ (T2); lista + busca + filtros + FAB ✓ (T3); criar/editar ✓ (T4); detalhe aba Info + ações contato (WhatsApp/Ligar) ✓ (T5); navegação bottom tabs ✓ (T6); deploy ✓ (T7). Abas Equipe/Acessos/Documentos e matriz de permissões completa são P3–P5 (decomposição registrada).

**2. Placeholder scan:** sem TBD/TODO; código completo em todos os steps; comandos com saída esperada.

**3. Type consistency:** `Cliente`/`ClienteInput` definidos em T2 e usados em T3/T4/T5; `listClientes(busca)`, `getCliente(id)`, `createCliente`, `updateCliente(id, input)`, `deleteCliente(id)` mesma assinatura entre serviço (T2), testes e páginas. Rotas `/clientes`, `/clientes/novo`, `/clientes/:id`, `/clientes/:id/editar` consistentes entre ListPage (push), AppTabs (Route) e DetailPage (Editar). `useHistory`/`useParams` de react-router v5 (versão do projeto).

**Premissa:** regras da coleção exigem apenas autenticação (sem matriz de roles ainda — isso entra no P3). Aceitável para P2 isolado: qualquer usuário Wenox logado gerencia clientes.
