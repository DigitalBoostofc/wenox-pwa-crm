/** Reexport — helpers de área padrão (Tarefas e Projetos). */
export {
  areaPadraoSincrona,
  areaTarefasSincrona,
  casarTipoProjeto,
  pathTarefasPadrao,
  pathTarefasSincrono,
  pathProjetosPadrao,
  pathProjetosSincrono,
} from '@/lib/rotaPadraoArea';
