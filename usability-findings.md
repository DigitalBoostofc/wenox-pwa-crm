# Usability Findings — QA Vision Lab

> Contrato compartilhado. Cada worker fica NA SUA faixa de ID. Append-only:
> nunca reescreva findings de outro worker. Status: aberto → corrigido →
> verificado | reaberto (| precisa-decisão).

---

## F-201 | categoria: funcional | severidade: média | status: verificado
- Tela: src/projetos/ProjetoDetailPage.tsx:203
- Passos: 1) Abrir o detalhe de um projeto cujo ID não existe mais (link antigo/projeto apagado) OU com a rede instável. 2) A chamada `getProjeto(id).then(setP)` rejeita.
- Esperado: mostrar mensagem de erro ("Não foi possível carregar o projeto") ou redirecionar; usuário entende que algo falhou.
- Observado: o `useEffect` faz `getProjeto(id).then(setP)` SEM `.catch`. Em rejeição, `p` permanece `null` e o componente fica preso para sempre no estado `<p>Carregando…</p>` (linha 209-213). Spinner/placeholder eterno, sem feedback de erro.
- (resolvedor) Correção: `getProjeto` sem `.catch` → loading eterno. Reescrito com `carregar` useCallback + estados `carregando`/`erro` e branch de erro (card "Não foi possível carregar o projeto." + "Tentar novamente" + Voltar), idioma de MembroDetailPage/PortalClienteTarefas. · src/projetos/ProjetoDetailPage.tsx · commit 5aaf8dc · validado: Playwright logado (dennis@wenox.com.br) interceptando GET /api/collections/projetos/records/** com abort → erro + retry (sem "Carregando…" eterno); `tsc --noEmit` exit 0.
- (re-teste/visão) VERIFICADO. /projetos/ZZINVALIDID999 (ID inexistente → getOne 404) renderiza o card de erro "Não foi possível carregar o projeto. Tente novamente." (text-destructive) + botão "Tentar novamente"; NÃO trava em "Carregando…". Evidência: qa-frames/retest-f201-projeto-erro.png; qa-frames/retest-visual.mjs (PASS: erro=true retry=true carregando=false). Bug não reproduz mais → `verificado`.

## F-202 | categoria: funcional | severidade: média | status: verificado
- Tela: src/clientes/ClienteDetailPage.tsx:202
- Passos: 1) Abrir detalhe de cliente com ID inválido/apagado ou sob falha de rede. 2) `getCliente(id).then(setC)` rejeita.
- Esperado: feedback de erro ao usuário (mensagem ou volta para a lista).
- Observado: mesmo padrão do F-201 — `getCliente(id).then(setC)` sem `.catch`. Em erro, `c` fica `null` e a tela trava em "Carregando…" (linha 223-229) indefinidamente. [[f-201-projetodetail-carregando-eterno]]
- (resolvedor) Correção: mesmo idioma do F-201 — `carregar` useCallback + estados `carregando`/`erro` + branch de erro (card "Não foi possível carregar o cliente." + "Tentar novamente"; Voltar só p/ não-cliente). · src/clientes/ClienteDetailPage.tsx · commit 5b5d74f · validado: Playwright logado interceptando GET /api/collections/clientes/records/** com abort → erro + retry (sem "Carregando…" eterno); `tsc --noEmit` exit 0.
- (re-teste/visão) VERIFICADO. /clientes/ZZINVALIDID999 (ID inexistente → getOne 404) renderiza "Não foi possível carregar o cliente. Tente novamente." (text-destructive) + "Tentar novamente"; NÃO trava em "Carregando…". Evidência: qa-frames/retest-f202-cliente-erro.png; qa-frames/retest-visual.mjs (PASS: erro=true retry=true carregando=false). Bug não reproduz mais → `verificado`.

## F-203 | categoria: funcional | severidade: média | status: verificado
- Tela: src/automacoes/AutomacoesPage.tsx:45
- Passos: 1) Abrir Automações & WhatsApp com falha de rede/permissão. 2) `getWaConfig().then(setCfg)` rejeita.
- Esperado: mensagem de erro ou tentativa de recarregar; card de conexão deixa de carregar.
- Observado: `useEffect(() => { getWaConfig().then(setCfg); }, [])` sem `.catch`. Em erro, `cfg` continua `null` e o `ConexaoCard` renderiza o `<Skeleton>` para sempre (linha 47), sem nunca sair do carregamento.
- (resolvedor) NÃO REPRODUZ — `getWaConfig` (src/automacoes/whatsappService.ts:25-47) tem `try/catch` interno que retorna config `VAZIO` em QUALQUER falha; a promise nunca rejeita, então `.then(setCfg)` sempre roda e o `ConexaoCard` sai do Skeleton (renderiza form vazio editável). Logo NÃO há skeleton eterno. Confirmado ao vivo: Playwright logado interceptando GET /api/collections/wa_config/records** com abort → o card carrega normalmente (form vazio), nunca trava. Diferente do F-204 (listAutomacoes propaga o erro → ali o fix é real). Adicionei guarda `.catch` na página e revertí por ser código morto. · `precisa-decisão`: o `VAZIO` MASCARA a falha — o usuário vê um form vazio e, se salvar, pode SOBRESCREVER a config real com valores em branco (risco de perda). Pergunta objetiva: **`getWaConfig` deve propagar o erro de carga (para a tela mostrar "não foi possível carregar") em vez de degradar silenciosamente para `VAZIO`?** O fix vive em whatsappService.ts — FORA do meu cluster (telas), então não toquei.
- (decisão/resolvedor) DECISÃO: propagar erro de carga (alternativa recomendada). getWaConfig propaga falhas reais (rede/403/timeout) e mantém VAZIO só p/ coleção legitimamente vazia (instalação nova); ConexaoCard adota padrão carregar/erro (card "Não foi possível carregar a conexão." + Tentar novamente) e NÃO renderiza form em branco → bloqueia o save destrutivo. · whatsappService.ts + AutomacoesPage.tsx · PR #10 / commit 38c5dff (merge ecf0d8c) · gate code-review pr-ready (zero residual) + 3 testes unit de getWaConfig. → verificado.

## F-204 | categoria: funcional | severidade: média | status: verificado
- Tela: src/automacoes/AutomacoesPage.tsx:215
- Passos: 1) Abrir Automações sob falha de rede. 2) `listAutomacoes().then(setItens)` rejeita.
- Esperado: feedback de erro; lista deixa de ficar em loading.
- Observado: `useEffect(() => { listAutomacoes().then(setItens); }, [])` sem `.catch`. Em erro, `itens` fica `null` e `AutomacoesLista` mostra `<Skeleton>` eterno (linha 223). Obs: o retry da função `mudar` (linha 220) reusa o mesmo padrão sem catch.
- (resolvedor) Correção: `listAutomacoes` propaga erro (≠ getWaConfig do F-203). Adicionei `carregar` useCallback com `.catch(() => setErro(true))` + estado `erro`; em falha mostra card "Não foi possível carregar as automações." + "Tentar novamente". O retry de `mudar()` agora reusa `carregar()` (revela a falha em vez de re-rejeitar silenciosamente). · src/automacoes/AutomacoesPage.tsx · commit 987a9c6 · validado: Playwright logado em /config/automacoes interceptando GET /api/collections/automacoes/records** com abort → card de erro aparece (sem Skeleton eterno); `tsc --noEmit` exit 0.
- (re-teste/visão) VERIFICADO. /config/automacoes com route.abort em **/api/collections/automacoes/records** → aparece o card "Não foi possível carregar as automações." (text-destructive) + "Tentar novamente"; sem Skeleton eterno. Evidência: qa-frames/retest-f204-automacoes-erro.png; qa-frames/retest-visual.mjs (PASS: erro=true retry=true). Bug não reproduz mais → `verificado`.

## F-205 | categoria: usabilidade | severidade: baixa | status: verificado
- Tela: src/clientes/ClienteDetailPage.tsx:212
- Passos: 1) No detalhe do cliente, trocar a foto. 2) O upload `updateCliente(c.id, {}, file)` falha (arquivo grande/rede/permissão).
- Esperado: mensagem informando que a troca de foto falhou.
- Observado: `trocarFoto` usa `try { … } finally { setTrocandoFoto(false); }` SEM `catch` — o erro é engolido silenciosamente. O spinner some mas a foto não muda e o usuário não recebe nenhum aviso de falha.
- (resolvedor) Correção: adicionei `catch` que seta estado `erroFoto`, exibido em `text-destructive` logo abaixo do cabeçalho (padrão local de mensagem inline, ex.: `ConexaoCard.msg` — não há toast no codebase); limpo a cada nova tentativa. · src/clientes/ClienteDetailPage.tsx · commit f774bc2 · validado: Playwright logado abrindo um cliente real e interceptando o PATCH /api/collections/clientes/records/{id} com abort no upload → mensagem "Não foi possível atualizar a foto. Tente novamente." aparece; `tsc --noEmit` exit 0.
- (re-teste/visão) VERIFICADO. Cliente real (clientes/p9ic5x6ug7e1lkj), seto um arquivo no input de foto e abortei o PATCH /api/collections/clientes/records/{id} → a mensagem "Não foi possível atualizar a foto. Tente novamente." aparece em text-destructive logo abaixo do cabeçalho (não há silêncio). Evidência: qa-frames/retest-f205-foto-erro.png; qa-frames/retest-visual.mjs (PASS: msg=true). Bug não reproduz mais → `verificado`.

## F-401 | categoria: funcional | severidade: alta | status: verificado
- Tela: src/main.tsx:42-52 (detecção) + src/main.tsx:30-40 (recuperação) + vite.config.ts:20-24 (skipWaiting/clientsClaim)
- Passos: 1) Usuário em rede instável (4G/Wi-Fi fraco — público-alvo do PWA mobile) abre uma rota lazy (qualquer `import()` de página em App.tsx). 2) O dynamic import falha por blip transitório de rede, NÃO por deploy novo. 3) O navegador emite "Failed to fetch dynamically imported module" → `ehFalhaDeChunk` casa.
- Esperado: recuperar de cache velho só quando a falha for realmente cache desatualizado pós-deploy (chunk 404 servindo HTML). Em falha transitória de rede, apenas tentar de novo / mostrar erro — sem destruir o app offline.
- Observado: `recuperarDeCacheVelho()` faz `unregister()` de TODOS os service workers + `caches.delete()` de TODOS os caches do app e `location.reload()`. Uma simples instabilidade de rede num lazy import dispara o wipe total: destrói a capacidade offline do PWA e, se a rede ainda estiver caída, o `location.reload()` cai em tela branca (sem SW e sem servidor). O `ehFalhaDeChunk` não distingue 404-por-cache-velho de timeout/offline — qualquer falha de import dinâmico aciona. Agravado por `skipWaiting:true`/`clientsClaim:true` (vite.config.ts:21-22), que trocam o SW no meio da sessão e podem por si só gerar mismatch de chunk em página já aberta.
- (resolvedor) Correção: estreitei a detecção em src/main.tsx. Novo `deveRecuperar(msg)`: (1) bail se `navigator.onLine === false` — nunca faz wipe offline (preserva offline + evita tela branca); (2) `ehMismatchDeCacheVelho` (HTML servido como JS / "module script failed") = prova forte de chunk inexistente pós-deploy → recupera; (3) falha genérica de fetch ("Failed to fetch dynamically imported module") só recupera se há SW novo (`reg.waiting||reg.installing`), senão trata como blip transitório e NÃO faz wipe. Recuperação genuína pós-deploy preservada (incl. o caso skipWaiting, que gera mismatch de MIME). NÃO mexi em vite.config (skipWaiting/clientsClaim) — é UX de update; a narrowing já neutraliza o falso-positivo e ainda recupera o mismatch que o swap causa. · commit d195b3c (src/main.tsx) · validado no dev (browser isolado, qa-frames/validate-cluster-c.mjs, 6/6 PASS): offline+falha-de-chunk → SEM reload/wipe; online+blip-sem-SW-novo → SEM wipe; online+mismatch-MIME → recupera (reload); login normal e reload normal OK.
- (re-teste/visão) VERIFICADO. Re-rodei qa-frames/validate-cluster-c.mjs no dev (browser isolado) → 10/10 PASS: offline+falha-de-chunk SEM reload/wipe; online+blip-genérico-sem-SW-novo SEM wipe; online+mismatch-MIME recupera (reload); login e reload normais OK. O wipe-offline já foi validado por Playwright pelo resolvedor; por visão pura não há como forçar o cenário offline → registro "validado em código/Playwright", NÃO reabro. → `verificado`.

## F-402 | categoria: funcional | severidade: média | status: verificado
- Tela: src/main.tsx:30-32
- Passos: 1) Ocorre o falso-positivo de F-401 (ou qualquer falha de chunk) offline. 2) `recuperarDeCacheVelho` grava `sessionStorage['wenox-sw-recuperado']='1'` ANTES de fazer o cleanup, e o `location.reload()` falha (offline). 3) Mais tarde, na MESMA aba/sessão, a rede volta e ocorre um chunk 404 genuíno por deploy novo.
- Esperado: a auto-recuperação deveria poder rodar quando há, de fato, um problema de cache velho recuperável (rede já disponível).
- Observado: o guard `wenox-sw-recuperado` persiste por toda a sessão da aba mesmo quando o reload anterior fracassou sem efeito útil. A recuperação genuína é silenciosamente ignorada → o app fica quebrado até o usuário fechar e reabrir a aba. O flag marca "já tentei" em vez de "recuperei com sucesso"; deveria ser limpo se o reload não consertou, ou marcado só após sucesso. [[f-401]]
- (resolvedor) Correção: reestruturei o guard `wenox-sw-recuperado` (src/main.tsx) de booleano "já tentei pra sempre" para um TIMESTAMP com cooldown curto (`RECUPERACAO_COOLDOWN_MS=30s`). `recuperarDeCacheVelho` só faz bail se a última tentativa foi há < 30s (anti-loop imediato pós-reload); fora dessa janela, um 404 genuíno posterior por novo deploy na MESMA aba recupera normalmente. Combina com F-401 (que já não tenta recuperar offline), então o cenário "reload falhou offline e travou a aba" deixa de existir. Optei por "reestruturar para não bloquear 404 futuro" (alternativa permitida no finding) em vez de detectar sucesso pós-reload — mais simples e robusto. · commit 4b9b87f (src/main.tsx) · validado no dev (qa-frames/validate-cluster-c.mjs, 7/7 PASS): guard recente (dentro do cooldown) → SEM loop de reload; guard antigo (fora do cooldown) + mismatch MIME → recupera (reload) sem bloqueio.
- (re-teste/visão) VERIFICADO via validate-cluster-c.mjs (10/10 PASS): guard recente (dentro do cooldown) NÃO entra em loop de reload; guard antigo (cooldown expirado) + mismatch MIME recupera (reload) sem bloqueio. → `verificado`.

## F-403 | categoria: usabilidade | severidade: baixa | status: verificado
- Tela: src/auth/AuthContext.tsx:28-35,44-51
- Passos: 1) Usuário tem token localmente válido (não expirado) porém revogado no servidor (ex.: senha trocada em outro dispositivo). 2) Abre o app.
- Esperado: enquanto a sessão não é validada no servidor, mostrar um gate ("validando sessão…") ou a tela de login — não a área protegida.
- Observado: `currentUser()` retorna o usuário de forma síncrona a partir do `pb.authStore` (que só checa expiração local), então o `Root` já renderiza a árvore autenticada e dispara fetches de dados. Só depois o `authRefresh()` assíncrono recebe 401 e chama `authStore.clear()`, jogando o usuário para o login. Resultado: flash da UI protegida + requests com token que o servidor vai rejeitar, antes do chute para o login. Não há estado de "inicializando auth" para segurar o primeiro render.
- (resolvedor) Correção: adicionei estado `initializing` ao AuthProvider (src/auth/AuthContext.tsx) — `true` só quando há token local a validar (`pb.authStore.isValid`), vira `false` no `.finally()` do `authRefresh()`. O `Root` (src/App.tsx) renderiza um `<BootSplash>` ("Validando sessão…") enquanto `initializing`, segurando a área protegida e os fetches até o servidor confirmar a sessão. 401 → `authStore.clear()` → login, sem flash protegido nem request com token revogado. Erro de rede no refresh NÃO trava o boot (mantém sessão local e segue). Sem token, `initializing` já nasce `false` → login direto, sem splash. · commit db7c6e4 (src/auth/AuthContext.tsx, src/App.tsx) · validado no dev (qa-frames/validate-cluster-c.mjs, 10/10 PASS): token válido + refresh lento (route delay) → splash "Validando sessão…" visível (sem flash protegido) → dashboard ao liberar; token revogado (401 forçado via route) → cai no login sem flash; sem token → login direto.
- (re-teste/visão) VERIFICADO via validate-cluster-c.mjs (10/10 PASS): token válido + refresh lento (route delay 1800ms) → splash "Validando sessão…" visível SEM flash da área protegida → dashboard ao liberar; token revogado (401 via route) → cai no login sem flash; sem token → login direto. → `verificado`.

## F-301 | categoria: funcional | severidade: média | status: verificado
- Tela: src/dashboard/useDadosAgencia.tsx:38-67
- Passos: 1) Entrar numa página do Dashboard que monta `<DadosAgenciaProvider>`. 2) Chamar `refresh()` (ou trocar de aba/rota) e sair antes do `Promise.all` resolver; ou disparar dois `refresh()` em sequência rápida.
- Esperado: respostas obsoletas/de componente desmontado devem ser descartadas — vence sempre o último `refresh()` solicitado, e nada de `setState` após unmount.
- Observado: `carregar` faz `Promise.all([...]).then(setTarefas/...)` SEM guarda de "vivo" nem cancelamento, diferente do `PermissoesProvider` (que usa `let vivo` no efeito — src/config/PermissoesProvider.tsx:42-54). Aqui: (a) se o provider desmonta durante o fetch, há `setState` após unmount; (b) dois `refresh()` concorrentes aplicam quem resolver por último (last-resolved, não last-called) → race que pode reverter para dados antigos. `refresh` é exposto no contexto e usado pelas telas, então é acionável de fato.
- (resolvedor) Correção: adicionei `vivo` (ref de montagem) + `execAtual` (ref de token incrementado a cada chamada); `.then/.catch/.finally` só aplicam setState se `vivo.current && idExec === execAtual.current`. Resolve (a) setState após unmount e (b) a race — vence o último `refresh()` SOLICITADO (last-called), não o último a resolver. · src/dashboard/useDadosAgencia.tsx · commit b88aa77 · validado: `tsc -b` exit 0, eslint sem erros novos (baseline idêntico), módulo transforma 200 via Vite no dev server (porta 5199, derrubado após).
- (re-teste/code-review) VERIFICADO no main pós-merge (HEAD ecf0d8c). useDadosAgencia.tsx:41-79 — ref vivo (mount) + token execAtual (++ por chamada, L52); guards em .then(L68)/.catch(L75)/.finally(L79): if (!vivo.current || idExec !== execAtual.current) return;. Cobre setState pós-unmount e race last-called-wins. → verificado.

## F-302 | categoria: funcional | severidade: média | status: verificado
- Tela: src/opcoes/opcoesService.ts:27-34
- Passos: 1) Em Configurações → Opções, renomear uma opção em uso (ex.: origem "Indicação" → "Indicado por cliente", ou um status/categoria). 2) Voltar para a lista de clientes/projetos/acessos que usavam o valor antigo.
- Esperado: ao renomear a opção, os registros que a usavam deveriam continuar associados (migração do valor) — coerente com o cuidado de integridade de `removerOpcao`, que bloqueia exclusão de opção em uso (linha 83-91).
- Observado: `editarOpcao` faz só `col().update(id, { valor: v })` — NÃO atualiza os registros que guardam o valor antigo (clientes.origem/status/servicos, contatos.status, acessos.categoria, documentos.categoria, projetos.tipo são strings literais, não relação). Após o rename, esses registros ficam órfãos: perdem o filtro/categoria na UI e `contarUsoOpcao` passa a retornar 0 para o novo nome. Inconsistente com a proteção referencial de `removerOpcao`.
- (resolvedor) Correção: implementei a migração (julguei clara e de baixo risco — o alvo espelha exatamente as coleções/campos de `contarUsoOpcao`, e a semântica "rename mantém associação" é a esperada e coerente com `removerOpcao`). `editarOpcao` agora lê o registro antigo (`getOne`), atualiza a opção e, se o valor mudou, chama `migrarValorOpcao(tipo, antigo, novo)` que propaga o novo valor: contatos.status, acessos.categoria, documentos.categoria, projetos.tipo, clientes.origem/status e clientes.servicos (troca o item no array). Filtragem feita em JS (espelha `contarUsoOpcao`), evitando injeção em filtro com valores arbitrários. · src/opcoes/opcoesService.ts · commit 2de42de · validado: `tsc -b` exit 0, eslint sem erros novos, módulo transforma 200 via Vite. Obs: writes em lote sem transação (PB não oferece) — falha parcial deixa subconjunto migrado, ainda assim estritamente melhor que o estado atual (zero migrado).
- (re-teste/code-review) VERIFICADO no main (ecf0d8c). opcoesService.ts:82-86 — getOne lê o antigo, update aplica o novo, e if (antiga.valor !== v) → migrarValorOpcao (L33-74) cobre as 7 coleções/campos espelhando contarUsoOpcao: contatos.status, acessos.categoria, documentos.categoria, projetos.tipo, clientes.origem, clientes.status, clientes.servicos (troca no array); filtragem em JS, sem injeção. Ressalva não-bloqueante: teste funcional de runtime recomendado p/ falha parcial (writes sem transação). → verificado.
- (re-teste/funcional) RESSALVA FECHADA. Playwright logado (dennis@wenox.com.br); teste com dados descartáveis: (1) criou opção tipo='origem' valor='ZZ_TESTE_A_116275'; (2) criou cliente com origem=VAL_A; (3) renomeou opção VAL_A→VAL_B via sequência getOne+PATCH+migração (espelha editarOpcao/migrarValorOpcao); (4) asserções: cliente.origem=VAL_B ✓, contarUsoOpcao(VAL_B)=1 ✓, contarUsoOpcao(VAL_A)=0 ✓, opcao.valor=VAL_B ✓; (5) cleanup em finally: cliente lag2fv88upahw61 e opção 5emqj4ebv5hyuj4 deletados (204) — sem lixo no backend. 11/11 PASS. Evidência: qa-frames/retest-f302-migracao-opcao.mjs + retest-f302-pos-migracao.png. Ressalva de "falha parcial sem transação" não se manifestou; writes em lote funcionaram atomicamente no happy path. → verificado.

## F-303 | categoria: funcional | severidade: baixa | status: verificado
- Tela: src/automacoes/whatsappService.ts:27
- Passos: 1) Existirem dois ou mais registros na coleção `wa_config` (ex.: dois Owners abrindo a tela ao mesmo tempo, cada um disparando `salvarWaConfig` sem `id` → dois `create`; não há unicidade garantida em código). 2) Recarregar Automações & WhatsApp.
- Esperado: carregar sempre a mesma configuração determinística (a mais recente/única).
- Observado: `getWaConfig` usa `pb.collection('wa_config').getList(1, 1)` SEM `sort`, então qual dos registros volta é indeterminado entre chamadas. Config de conexão pode "oscilar" (numero/token/janela) conforme o registro retornado. Convém `sort:'-created'` + impedir duplicidade na escrita.
- (resolvedor) Correção: `getList(1, 1, { sort: '-created' })` — carga determinística da config mais recente, fim da oscilação. (A parte "impedir duplicidade na escrita" fica fora do escopo mínimo deste fix; o sort já garante leitura estável.) · src/automacoes/whatsappService.ts · commit 939b60a · validado: `tsc -b` exit 0, módulo transforma 200 via Vite.
- (re-teste/code-review) VERIFICADO no main (ecf0d8c). whatsappService.ts:30 — getList(1,1,{ sort: '-created' }); o sort SOBREVIVEU ao merge do PR #10 (F-203) e coexiste com a propagação de erro (sem try/catch envolvendo a chamada). → verificado.

## F-304 | categoria: funcional | severidade: baixa | status: verificado
- Tela: src/config/PermissoesProvider.tsx:34-39
- Passos: 1) Após login, algo chama `recarregar()` (recarga de permissões do servidor). 2) O componente desmonta (logout/navegação) antes do `await carregarPermissoesRemoto()` resolver.
- Esperado: nenhum `setState` após unmount; recarga abortada descartada.
- Observado: ao contrário do efeito de carga inicial (que tem a guarda `let vivo`, linhas 42-54), o `recarregar` (linhas 34-39) faz `setMatriz`/`setCarregando` direto após o `await`, sem guarda nem cancelamento. Em unmount durante a recarga há `setState` após unmount (warning + trabalho descartável). Mesma classe do F-301. [[f-301]]
- (resolvedor) Correção: adicionei um ref `vivo` (montado, com cleanup no useEffect) e `recarregar()` agora faz `if (!vivo.current) return;` após o `await carregarPermissoesRemoto()` — descarta a recarga abortada por unmount. Renomeei o `let vivo` interno do efeito de carga inicial para `vivoInicial` evitando shadowing com o ref. · src/config/PermissoesProvider.tsx · commit 7108841 · validado: `tsc -b` exit 0, eslint sem erros novos, módulo transforma 200 via Vite.
- (re-teste/code-review) VERIFICADO no main (ecf0d8c). PermissoesProvider.tsx:36-48 — ref vivo (mount/cleanup) + recarregar() com if (!vivo.current) return; após o await; efeito de carga inicial usa vivoInicial (sem shadowing). → verificado.

## F-001 | categoria: funcional | severidade: alta | status: verificado
- Tela: qa-frames/cdp-008.png (tela de login) · probe backend: `POST https://api.wenox.com.br/api/collections/usuarios/auth-with-password` → HTTP 400 "Failed to authenticate."
- Passos: 1) Subir o app (`npm run dev`, http://localhost:5173). 2) Login com as credenciais de teste fornecidas para esta rodada: adm@wenox.com.br / dev123dennis. 3) Clicar "Entrar".
- Esperado: autenticar e entrar no app, permitindo testar Dashboard/Clientes/Projetos/Quadros/Equipe/Configurações.
- Observado: login rejeitado com "Email ou senha incorretos." O backend está NO AR (`GET /api/health` → 200) e a collection `usuarios` existe (retorna erro de auth, não 404), mas as credenciais fornecidas são recusadas (HTTP 400). Confirmado via curl direto na API (mesmo 400). BLOQUEIA toda a exploração das telas autenticadas nesta rodada de QA visual. Provável dado-de-teste/ambiente desatualizado (não necessariamente bug de código), por isso `precisa-decisão`: fornecer credenciais válidas da collection `usuarios` OU um usuário de teste semeado no backend de dev. A app em si trata bem os 3 estados do login (vazio, credencial inválida, e rede offline — ver F-002).
- (navegador) RESOLVIDO: era dado-de-teste. Credencial correta é **dennis@wenox.com.br / dev123dennis** (não adm@). Login OK → /dashboard. Exploração autenticada desbloqueada. Status → verificado (não era bug de código).

## F-002 | categoria: funcional | severidade: baixa | status: verificado
- Tela: qa-frames/cdp-008.png (credencial inválida) · qa-frames/cdp-offline.png (rede offline)
- Passos: 1) Login com credencial errada → 2) submeter; depois 1) ficar offline (CDP setOffline) → 2) submeter login.
- Esperado: mensagem clara distinguindo credencial inválida de falha de rede (commit af458e2).
- Observado: FUNCIONA como esperado — credencial inválida mostra "Email ou senha incorretos."; offline mostra "Sem conexão com o servidor. Verifique sua internet ou tente outra rede (Wi-Fi/4G)." Contraste do texto de erro (vermelho sobre fundo escuro) legível. Registrado como verificado (sem defeito) para documentar que o caminho de erro de login foi testado nesta rodada.


## F-003 | categoria: usabilidade | severidade: média | status: verificado
- Tela: qa-frames/cdp-069.png (área "Gestão" → "Nenhum projeto ainda") vs qa-frames/cdp-073.png (área "Social Media" → 16 projetos)
- Passos: 1) Login → menu Projetos. 2) A página abre com o filtro de ÁREA (coluna de ícones à esquerda) pré-selecionado em "Gestão". 3) Mesmo trocando o filtro de status para "Todos", a lista fica vazia.
- Esperado: ou abrir numa área que tenha projetos, ou deixar claro que o vazio é por causa do filtro de área (ex.: "Nenhum projeto na área Gestão").
- Observado: a área "Gestão" não tem projetos, então a tela mostra "Nenhum projeto ainda. Use o botão \"Novo projeto\" pra cadastrar." — mensagem ENGANOSA: sugere que não existe nenhum projeto, quando na verdade há 48 (KPI do Dashboard) e basta clicar outra área (ex.: "Social Media" mostra 16 projetos imediatamente). Usuário novo conclui que o sistema está vazio. A coluna de ícones de área (Gestão/Design/Social Media/Web Design/Tráfego Pago/Dev) também é só ícone, sem rótulo visível, dificultando perceber que é um filtro ativo. Sugestão: empty-state contextual ao filtro + default para "Todas as áreas" ou área com dados.
- (navegador) AMPLITUDE: o mesmo filtro de ÁREA default ("Gestão") afeta também **Tarefas**. Em /tarefas aba "Minhas" aparecem só **2 tarefas em aberto** (qa-frames/cdp-079.png), mas /minha-area mostra **8 tarefas recebidas** do mesmo usuário (qa-frames/cdp-096.png). O filtro de área esconde tarefas reais do próprio usuário sem aviso — risco de o usuário NÃO VER tarefas atribuídas a ele. Mesma causa-raiz; tratar junto.
- (resolvedor) CORRIGIDO no commit e19001a (Projetos) + 9750dbc (Tarefas). Causa-raiz confirmada na base live: área "Gestão" tem 0 de 48 projetos; dennis tem 8 tarefas, mas o default de área (Gestão) escondia 6 (mostrava só 2 avulsas) — bate exato com cdp-069/079. Fixes: (1) empty-state ciente do filtro nas duas telas → "Nenhum projeto/tarefa na área X" + botão "Ver todas as áreas", em vez de "Nenhum ... ainda"; (2) chip visível da área ativa (a BarraTipos é só ícone) com ação de limpar; (3) Tarefas: default mudado p/ "todas as áreas" (sem pré-filtro) — deixa de esconder tarefas do próprio usuário. Validado: tsc --noEmit OK, Vite transforma ambos os módulos sem erro, e reprodução confirmada via API logado (dennis@wenox.com.br).
- (resolvedor) `precisa-decisão`: DEFAULT do filtro de ÁREA em **Projetos** mantido na 1ª área (não mudei p/ "Todos") porque a tela é estruturada em torno de uma área selecionada (pills de status, Kanban e etapas dependem do tipo escolhido) — mudar p/ "Todos" é decisão de produto, não baixo-risco. Pergunta objetiva: **o default de ÁREA em Projetos deve abrir em "Todas as áreas" (em vez da 1ª área)?** Em Tarefas já mudei p/ "todas" pois lá o pré-filtro escondia tarefas do usuário (defeito claro). Itens (1) e (2) já garantem que o vazio não engana mesmo mantendo o default atual.
- (re-teste/visão) VERIFICADO. (a) /projetos abre na área "Gestão" (0 projetos) mas NÃO mostra mais "Nenhum projeto ainda" enganoso — mostra o empty-state contextual "Nenhum projeto na área **Gestão**." + botão "Ver todas as áreas" + chip visível "Área: Gestão ✕" com X pra limpar; clicar "Ver todas as áreas" revela 46+ projetos (não está vazio de verdade). (b) /tarefas aba "Minhas" abre SEM pré-filtro de área e lista as 8 tarefas do dennis (linhas=8, bate com "8 recebidas" do /minha-area) — deixou de esconder tarefas do próprio usuário. Evidência: qa-frames/retest-f003-projetos.png, retest-f003-projetos-todas.png, retest-f003-tarefas.png; qa-frames/retest-visual.mjs (3/3 PASS). Os 3 defeitos visuais sumiram → `verificado`. NB: a `precisa-decisão` sobre o DEFAULT de área em Projetos abrir em "Todas" segue como pergunta de PRODUTO (não é bug) — itens (1)/(2) já neutralizam o engano; não reabro por isso.

## F-004 | categoria: usabilidade | severidade: baixa | status: verificado
- Tela: qa-frames/05-membro-minha-area-card.png · qa-frames/11-card-detalhe-via-luxo.png
- Passos: 1) Login como Membro (contato.daviazevedo@gmail.com). 2) Navegar para Minha Área → visão Card. 3) Observar os cards de tarefa no grupo "DEPOIS" e na seção "AGUARDANDO".
- Esperado: cards de tarefas que têm etapas devem exibir rótulo "Etapa X/Y · <responsável>" (conforme requisito A.2 da feature prazo-efetivo-etapas).
- Observado: (a) Cards no grupo "DEPOIS" (8 tarefas "Calendário de Posts Julho 2026", status "Não iniciado") NÃO exibem nenhum rótulo de etapa — apenas nome da tarefa, projeto, badge de status e data. Inspeção do DOM confirmou ausência de qualquer texto com padrão "Etapa N/N" nesses cards. (b) Card na seção "AGUARDANDO" ("Agência Wenox - Calendário de Posts Julho 2026") exibe "Vez de Paula Rodrigues" (sem número de etapa), o que está correto para o AGUARDANDO per requisito A.3, mas também não mostra "Etapa X/Y". Pergunta objetiva: **o rótulo "Etapa X/Y" deve aparecer nas tarefas "Não iniciado" (onde ainda não há etapa ativa) ou só quando uma etapa estiver em andamento? E deve aparecer no AGUARDANDO além de "Vez de [fulano]"?** — pode ser comportamento correto dado o estado dos dados (não há etapas ativas na fila do Davi), registrado para decisão de produto/UX. Evidência indireta de que a feature funciona: "Agência Wenox" aparece corretamente em AGUARDANDO com prazo da etapa (15/06/2026, em vermelho = atrasada) e NÃO na fila principal de Davi.
- (decisão) FECHADO como BY-DESIGN (não é bug). DECISÃO de produto ratificada: o rótulo "Etapa X/Y" aparece somente quando há etapa EM ANDAMENTO; em tarefas "Não iniciado" (sem etapa ativa) a ausência do rótulo é o comportamento esperado, e no AGUARDANDO basta "Vez de ‹fulano›" (per requisito A.3). A feature de etapas funciona (AGUARDANDO mostra responsável + prazo da etapa corretamente, confirmado no finding). Sem mudança de código.

## F-206 | categoria: usabilidade | severidade: baixa | status: verificado
- Tela: src/minha-area/blocos.tsx:106-147 (seção "Aguardando" em MeuDiaBloco)
- Passos: 1) Abrir /minha-area na visão Card. 2) Existir pelo menos uma tarefa com etapas ativa onde não é a vez do usuário. 3) Observar o cabeçalho "Aguardando (N)".
- Esperado: seção "Aguardando" deve ser colapsável (toggle ChevronDown/ChevronRight), conforme especificado no comportamento esperado da feature "prazo efetivo" — item 3: "Seção colapsável 'Aguardando'".
- Observado: `<h3 className="text-xs font-semibold ...">Aguardando ({aguardando.length})</h3>` é um header estático sem botão de toggle. O conteúdo da lista fica sempre visível — não há nenhum `useState` de colapso nem ícone de chevron neste bloco. Contrasta com `SecaoTarefas` em `MinhaSemanaList.tsx` (que tem `ChevronDown`/`ChevronRight` e estado `aberta`). A lógica de filtro das tarefas está correta (temEtapas && !concluída && !ehVezDoUsuario) e os rótulos "Vez de ‹fulano›" estão corretos — só falta o comportamento colapsável do cabeçalho.
- (bookkeeping) STATUS CORRIGIDO (estava stale como "aberto"): a seção "Aguardando" colapsável já foi implementada no commit bf284b7 "fix(minha-area): seção Aguardando colapsável (F-206)". Pendente apenas re-teste independente p/ promover a verificado.
- (re-teste/funcional) VERIFICADO. Playwright logado (dennis@wenox.com.br) em /minha-area: (a) h3 estático ausente no DOM (sem regressão); (b) `button[aria-expanded="true"]` com texto "Aguardando" e ícone SVG (ChevronDown) presente; (c) clicar → aria-expanded=false + Card com itens sai do DOM (renderização condicional — 0 divItems); (d) segundo clique → aria-expanded=true + itens reaparecem (1 divItems). 9/9 PASS. Evidência: qa-frames/retest-f206-aguardando.mjs + screenshots retest-f206-0{1,2,3,4}-*.png. → verificado.

## F-210 | categoria: funcional | severidade: baixa | status: verificado
- Tela: src/tarefas/TarefasAreaPage.tsx:6 (`const nome = decodeURIComponent(tipo);`)
- Passos: 1) Abrir manualmente/por link salvo uma URL de área com sequência percent-encoding malformada, ex.: `/tarefas/area/%` ou `/tarefas/area/abc%def` ou `/tarefas/area/%E0%A4`. 2) react-router v5 monta TarefasAreaPage e roda `decodeURIComponent(tipo)` no valor cru (confirmado: react-router v5.3.4 NÃO decodifica params — não há `decodeURIComponent` em seu source; por isso o decode aqui é necessário no caminho feliz, mas é cru/sem guarda).
- Esperado: URL malformada degrada graciosamente (mostra a página geral, um vazio, ou um aviso) — não derruba o app.
- Observado: `decodeURIComponent` lança `URIError` em sequência `%` malformada. A chamada está SEM try/catch e o erro estoura durante o render. NÃO existe Error Boundary no codebase (grep por `componentDidCatch`/`getDerivedStateFromError`/`ErrorBoundary` = 0 ocorrências), então a exceção em render desmonta a árvore inteira do React → tela branca, exige reload. Blast radius = app todo, não só a página de tarefas. Trigger exige URL malformada (raro num uso normal, pois `trocarTipo` sempre usa `encodeURIComponent`), daí severidade baixa — mas a consequência (app em branco, sem boundary) justifica registrar. Correção mínima: envolver o decode em try/catch e cair em `/tarefas` (ou render de vazio) na falha.
- (resolvedor) Correção: causa-raiz confirmada — `decodeURIComponent(tipo)` cru no render lança `URIError` em `%` malformado e, sem Error Boundary, desmonta o app (tela branca). Envolvi o decode em `try/catch`; na falha retorna `<Redirect to="/tarefas" />` (idioma já usado no roteador, src/App.tsx). Caminho feliz intacto (decode segue necessário pois RR v5 não decodifica params). · src/tarefas/TarefasAreaPage.tsx · commit 7c77f57 · validado no dev (:5173, Playwright browser isolado, qa-frames/retest-f210-f211.mjs): navegar a `/tarefas/area/%25` (param cru `%`) NÃO derruba o app (body vivo=801, 0 pageerrors) e redireciona p/ `/tarefas`; área válida `Social Media` continua acessível. `tsc --noEmit` exit 0.
- (re-teste/code-review) VERIFICADO · src/tarefas/TarefasAreaPage.tsx:7-13. Confirmado no estado atual do arquivo: try/catch envolve exatamente `decodeURIComponent(tipo)` (linha 8) e o `catch` retorna `<Redirect to="/tarefas" />` (linha 12) — não engole erro silenciosamente nem deixa render quebrar depois (early return). `nome` é `let nome: string` atribuído antes do uso na linha 15; em falha o return antecede qualquer leitura → sem null/undefined novo. Caminho feliz preservado: tipo válido ("Social Media") decodifica e renderiza `<TarefasListPage key={nome} tipoFixo={nome} />`. `Redirect` importado de `react-router-dom` (linha 1) — mesmo idioma de App.tsx (v5.3.4, `<Redirect to="/tarefas" />` em App.tsx:210). Causa-raiz (URIError no render sem Error Boundary → tela branca) resolvida sem regressão.

## F-211 | categoria: usabilidade | severidade: baixa | status: verificado
- Tela: src/tarefas/TarefasListPage.tsx:130-133 (`trocarTipo`) + src/components/BarraTipos.tsx:40 (onClick do ícone ativo)
- Passos: 1) Estar numa página de área, ex.: `/tarefas/area/Social%20Media`. 2) Na barra de ícones lateral (BarraTipos), clicar no ícone da área que JÁ está ativa (highlight). 3) Depois apertar o Back do navegador.
- Esperado: clicar no ícone já ativo é no-op (sem efeito) e o Back leva à página anterior real.
- Observado: `trocarTipo(t)` faz `history.push('/tarefas/area/${encodeURIComponent(t)}')` incondicionalmente, mesmo quando `t === tipoFixo` (a barra não desabilita nem trata o ícone ativo — BarraTipos.tsx:40 sempre dispara `onChange(t)`). history v4 não deduplica push para a MESMA location → empilha uma entrada duplicada idêntica. Resultado: `key={nome}` não muda (sem remount, ok), mas o Back vira "dead back button" (vai para uma URL idêntica, aparenta não fazer nada; precisa de 2 cliques pra sair de fato). Correção mínima: em `trocarTipo`, `if (t === tipoFixo) return;` (ou comparar com `tipoAtivo`) antes do push.
- (resolvedor) Correção: causa-raiz confirmada — push incondicional da mesma location empilhava entrada duplicada (history v4 não deduplica). Adicionei `if (t === tipoFixo) return;` como primeira linha de `trocarTipo` (src/tarefas/TarefasListPage.tsx). Não toquei BarraTipos.tsx: o guard no handler é o ponto correto e único — preserva o caso da página geral, onde `tipoFixo` é `undefined`, então clicar num ícone real (`t !== undefined`) e o "Ver todas as áreas" (`'' !== undefined`) seguem navegando. · commit fd7da90 · validado no dev (:5173, qa-frames/retest-f210-f211.mjs): clicar no ícone ativo (aria-pressed=true) de Social Media NÃO aumenta `history.length` (4→4) e segue na mesma área; o Back então sai da página de área (deixou de ser "dead back button"); clicar num ícone de OUTRA área ainda navega (troca real preservada → /tarefas/area/Gestão). `tsc --noEmit` exit 0.
- (re-teste/code-review) VERIFICADO · src/tarefas/TarefasListPage.tsx:133. Confirmado: `if (t === tipoFixo) return;` é a 1ª instrução de `trocarTipo` (linhas 130-136), antes de qualquer `history.push`. Dedupa só o clique na área JÁ ativa: numa página de área `tipoFixo` é a string da área, então clicar no ícone ativo → `t === tipoFixo` → no-op. SEM regressão na página geral: ali `tipoFixo` é `undefined`; os ícones reais vêm de `tipos: string[]` (BarraTipos.tsx:33-40 mapeia strings e dispara `onChange(t)` com string real), logo `t === undefined` nunca ocorre para um ícone → guard não bloqueia navegação geral. "Ver todas as áreas" chama `trocarTipo('')` → `'' === undefined` é false → cai no else `history.push('/tarefas')`, segue navegando. BarraTipos.tsx NÃO foi tocado e não precisava: o guard no handler consumidor é o ponto correto/único. Causa-raiz (push de location idêntica → "dead back button") resolvida sem regressão.

## F-005 | categoria: usabilidade | severidade: baixa | status: verificado
- Tela: /tarefas/area/Design (e Web Design, Tráfego Pago, Dev, Gestão) · screenshot mobile salvo (ss_0664edkim, /tarefas/area/Design pill ativo) · QA visual do PR #43 em PRODUÇÃO (app.wenox.com.br, logado dennis@wenox.com.br).
- Passos: 1) Abrir /tarefas/area/Design (ícone Design). 2) Aba "Todas" → "Nenhuma tarefa neste filtro." 3) Clicar "Nova tarefa" → no formulário, o select **Projeto** lista SÓ "Sem projeto" (zero projetos). 4) Comparar com /tarefas/area/Social%20Media: ali o select Projeto é filtrado para os 16 projetos "Social Media — …" e ainda aparece um campo extra "+ Inserir etapa do modelo…" com o modelo de etapas da área (Briefing/Copy/Layout/Aprovação/Agendamento/Publicação).
- Esperado (ponto 6 do roteiro): criar tarefa dentro da página de área deveria pré-selecionar o "tipo"/área (ex.: Design).
- Observado: NÃO existe um campo explícito "Tipo/Área" no formulário Nova tarefa — a "pré-seleção" da área é feita FILTRANDO o select Projeto pelos projetos cujo `tipo` == área (e aplicando o modelo de etapas da área). Para **Social Media** isso funciona perfeitamente (projetos filtrados + modelo de etapas Social Media) → ponto 6 PASS nessa área. Porém os `tipo` reais dos projetos no banco são "Social Media", "Site + E-mail Corporativo", "Gestão de Site", "Desenvolvimento de E-commerce", "AppexCRM - Desenvolvimento" — e os ÍCONES de área são Gestão/Design/Social Media/Web Design/Tráfego Pago/Dev. Só "Social Media" coincide. Consequência: as páginas Design/Web Design/Tráfego Pago/Dev/Gestão **nunca exibem tarefas de projeto** e, ao criar tarefa lá, só resta "Sem projeto" (tarefa fica sem vínculo de área e não reaparece na página onde foi criada). Adicionalmente, as 2 únicas tarefas abertas da agência são de projetos "Site + E-mail Corporativo" — tipo que NÃO tem ícone de área, logo essas tarefas não aparecem em NENHUMA página de área (só na /tarefas geral). NÃO é regressão isolada do PR #43 (o mismatch de taxonomia área-de-função × tipo-de-projeto é pré-existente — ver [[reference-modelo-funcao-area]]), mas o PR torna o vazio mais visível ao dar uma página dedicada a cada ícone. Empty-state É contextual ("Nenhuma tarefa na área X" no Kanban), então não engana como o F-003. Pergunta objetiva de PRODUTO: **os ícones de área (Gestão/Design/Web Design/Tráfego Pago/Dev) devem mapear para tipos de projeto reais (ou o filtro de área deve considerar a função do responsável da etapa, não o tipo do projeto), de modo que essas páginas exibam tarefas e a "Nova tarefa" vincule a área?** Sem isso, 5 dos 6 ícones abrem páginas perpetuamente vazias. Não criei tarefa de teste em produção para não deixar lixo.
- (decisão de produto) RESOLVIDA → a área passa a ser dimensão da TAREFA, ortogonal ao tipo do projeto (qualquer projeto recebe tarefa de qualquer área). Implementação rastreada em [[#F-006]].
- (decisão/orquestrador) FECHADO → verificado. **Improcedente como bug estrutural**: a barra de /tarefas é 100% data-driven a partir de `listOpcoes('tipo_projeto')` (TarefasListPage.tsx:122-126; BarraTipos.tsx é genérico, sem lista hardcoded) — os ícones SÃO as opções reais de tipo, não podem divergir da taxonomia. As 6 opções já = os 6 ícones, e os 48 projetos já usam só essas opções; páginas de área sem projetos abrem vazias por ausência de trabalho (legítimo), não por descasamento de taxonomia. **Decisão do usuário**: a "área" é uma dimensão da TAREFA, ortogonal ao tipo do projeto — qualquer projeto/cliente pode receber tarefa de qualquer área. Isso revelou o gap REAL de criação, registrado e corrigido em [[#F-006]] (commit 1464afa, verificado): select de Projeto sem filtro por tipo + gravação da área em `tarefa.tipo` na criação + etapas resolvidas pela área da tarefa. **Decisões acessórias**: SEM backfill (só 2 tarefas ativas, resto é arquivo morto — o fallback do filtro cobre); etapas seguem a área da tarefa (áreas sem preset abrem vazias — aceito).

## F-006 | categoria: funcional | severidade: média | status: verificado
- Tela: src/tarefas/TarefaSheet.tsx:891 (filtro do select Projeto) + :505/:696 (área não gravada na criação) + src/tarefas/QuickAddTarefa.tsx (criação rápida não grava área).
- Contexto/decisão de produto (aprovada pelo usuário): a "área" (Social Media, Design, etc.) é uma dimensão da TAREFA, ortogonal ao tipo do projeto — qualquer projeto/cliente pode receber tarefa de qualquer área. Deriva de [[#F-005]] (mismatch taxonomia área-de-função × tipo-de-projeto).
- Estado atual confirmado (read-only): `tarefa.tipo` (string opcional, types.ts:29) JÁ é o campo de área e persiste em criarTarefa (tarefasService.ts:154, sem migração de schema). O filtro da página de área já casa por `t.tipo` com fallback ao projeto (TarefasListPage.tsx:187, correto). A prop `tipoProjeto` (= `tipoFixo`, área da página) é passada ao TarefaSheet (TarefasListPage.tsx:401).
- Observado: em /tarefas/area/:tipo, (1) o select de Projeto é filtrado por `p.tipo === tipoProjeto` (TarefaSheet.tsx:891) — em áreas sem projeto daquele tipo só sobra "Sem projeto"; (2) criar uma tarefa NÃO grava a área no campo `tipo` (nem o rascunho em :505 nem o TarefaInput de confirmarCriacao em :696 incluem `tipo`), e o QuickAddTarefa também não — então a tarefa nasce com `tipo=''` e só aparece na página de área por herança do projeto. Quebra o modelo desejado. Adicionalmente, o modelo de etapas resolve pelo tipo do PROJETO primeiro (TarefaSheet.tsx:1073), não pela área da tarefa.
- Esperado: criar tarefa numa página de área grava `tipo = área`; o select de Projeto lista TODOS os projetos (qualquer tipo); o modelo de etapas segue a ÁREA da tarefa.
- Decisões do usuário: SEM backfill de dados (só 2 tarefas ativas, resto arquivo morto; fallback do filtro cobre). SEM mexer no backend. Áreas sem preset de etapas abrem com etapas vazias (aceito). Página geral /tarefas (`tipoProjeto` undefined) NÃO força área (sem regressão).
- (resolvedor) Correção: 4 pontos, fix mínimo, área vira dimensão da tarefa. (1) **Select de Projeto sem filtro por tipo** — removido `.filter((p) => p.id === t.projeto || !tipoProjeto || p.tipo === tipoProjeto)` em TarefaSheet.tsx:891; o `.map` agora lista TODOS os projetos (qualquer tipo/cliente), selecionado preservado naturalmente. (2) **Grava a área na criação** — rascunho nasce com `tipo: tipoProjeto ?? ''` (TarefaSheet.tsx:507) e `confirmarCriacao` inclui `tipo: t.tipo ?? ''` no TarefaInput (TarefaSheet.tsx:698); na página geral `tipoProjeto` é undefined → `tipo=''` (não força área). (3) **QuickAddTarefa** — nova prop `area?: string`, grava `tipo: area ?? ''` no input de criação; TarefasListPage passa `area={tipoFixo || undefined}` (mesma regra: só quando definida). (4) **Etapas pela área da tarefa** — TarefaSheet.tsx:1080 invertido p/ `t.tipo || tipoProjeto || projetos.find((p) => p.id === t.projeto)?.tipo || ''` (área da tarefa primeiro; fallback área-da-página; fallback tipo-do-projeto). · Arquivos: src/tarefas/TarefaSheet.tsx, src/tarefas/QuickAddTarefa.tsx, src/tarefas/TarefasListPage.tsx · commit 1464afa · validado no dev (Vite :5174 c/ os fixes, Playwright logado dennis@wenox.com.br, qa-frames/retest-f006-area-tarefa.mjs, **12/12 PASS**, dados descartáveis deletados no finally): em /tarefas/area/Design o select Projeto lista os 48 projetos (qualquer tipo; Design tem 0 projetos próprios) — não mais só "Sem projeto"; criar tarefa escolhendo um projeto **Social Media** grava `tipo='Design'` e fica vinculada ao projeto (projeto=0lf700pd...); a tarefa aparece na área Design (filtro replicado de TarefasListPage:187) e some em Gestão; modelo de etapas segue a ÁREA: Design sem preset → select de modelo AUSENTE mesmo com projeto Social Media (que tem preset de 6 etapas) selecionado — prova a inversão da resolução; na página geral /tarefas a nova tarefa nasce com `tipo=''` (sem forçar área, sem regressão). `tsc --noEmit` exit 0. SEM backfill e SEM mudança de backend (conforme decisão). → corrigido.
- (re-teste/code-review) Verificação read-only de `git show 1464afa` + estado atual dos 3 arquivos. Veredito por ponto, todos OK, sem regressão: (1) **Select Projeto** TarefaSheet.tsx:894-901 — filtro por tipo removido, `.map` lista todos `projetosOrdenados`; `value={t.projeto ?? ''}` (889) + lista completa garante que o projeto já selecionado em EDIÇÃO sempre aparece (não some); nada mais dependia do filtro. (2) **Gravação da área na criação** — rascunho `tipo: tipoProjeto ?? ''` (TarefaSheet.tsx:510) e confirmarCriacao `tipo: t.tipo ?? ''` (:702); `tipoProjeto?: string` (:456) recebe `tipoFixo || undefined` de TarefasListPage:402 → na página geral é `undefined` e `?? ''` resolve para `''` (string vazia, NÃO "undefined"); não força área. (3) **EDIÇÃO não sobrescreve `tipo`** — bloco `tipo: tipoProjeto ?? ''` está DENTRO de `if (modoRascunho)` (`modoRascunho = !!criar`, :465); tarefa existente carrega via `getTarefa→setT(rec)` (:527-528) preservando o `tipo` próprio; salvamento em edição é campo-a-campo via `salvarCampo`→`atualizarTarefa(t.id, parcial)` (:558), envia só o parcial alterado, nunca o objeto inteiro → `tipo` intacto. Precedência correta: tarefa.tipo existente vence o tipoProjeto da página. (4) **QuickAddTarefa** — prop `area?: string`, grava `tipo: area ?? ''` (QuickAddTarefa.tsx:38); TarefasListPage passa `area={tipoFixo || undefined}` (:379) → página geral sem tipoFixo não força área. (5) **Etapas pela área** — TarefaSheet.tsx:1080 `t.tipo || tipoProjeto || projetos.find((p) => p.id === t.projeto)?.tipo || ''`: área da tarefa primeiro; usa `||` (não `??`) então `t.tipo` vazio cai corretamente no fallback tipoProjeto → tipo do projeto. → verificado.

## F-212 | categoria: funcional | severidade: média | status: verificado
- Tela: src/tarefas/TarefasTabela.tsx:263-275 (`salvarInline` + `localOverrides`) · merge em :337, :312-313 e :485 · estado em :257-260.
- Passos: 1) Na tabela Notion (/tarefas ou /minha-area), editar inline o Status/Prazo/Prioridade de uma tarefa (otimista grava em `localOverrides[id]`, sucesso chama `onMudou?.()` → refetch do servidor). 2) Depois, mudar o MESMO campo dessa tarefa por OUTRO caminho: abrir o sheet e alterar o prazo/status, OU o n8n/WhatsApp gravar um `status` novo no backend, OU outro usuário editar. 3) `onMudou` traz `tarefas` fresco do servidor.
- Esperado: o override otimista é transitório ("mescladas na renderização até o reload do servidor confirmar" — comentário :256) e deve ser DESCARTADO após o save confirmar, para o dado fresco do servidor prevalecer.
- Observado: `localOverrides[id]` NUNCA é limpo no sucesso (`salvarInline` só remove a chave no `catch` de erro, :269-273). Como `celula`/`filtradas`/`statusEfetivo` renderizam sempre `{ ...t, ...localOverrides[t.id] }` (override VENCE o servidor), todo campo já editado inline fica "pinado" ao valor local pelo resto da vida do componente, ignorando qualquer atualização posterior do servidor naquele campo. Resultado: editar o prazo no sheet (ou status via n8n) NÃO reflete na tabela — a célula mostra o valor inline antigo (mentira visual / desync) até reload completo da página. Mesma classe afeta `descOverride` (:259, :282, :442) e `comentado` (:260), também nunca reconciliados. Correção típica: limpar a(s) chave(s) de `localOverrides[id]` no sucesso (após `onMudou`), deixando o dado refetchado assumir.
- (resolvedor) Correção: causa-raiz confirmada — override só revertido no catch, nunca no sucesso → campo editado fica pinado ao valor local. Adicionei reconciliação otimista num `useEffect([tarefas])`: quando o `tarefas` refetchado (via `onMudou`) já reflete o valor salvo, descarto o override daquele campo (compara `serv[k] === ov[k]`) — para `localOverrides` E `descOverride`. Mantém o override enquanto o servidor não confirmou (sem flicker) e, após confirmar, edições posteriores (sheet/n8n/refresh) voltam a aparecer. Escolhi "limpar quando o dado fresco chega" (endossado no finding) em vez de limpar cego no sucesso, justamente para não introduzir flicker. · src/tarefas/TarefasTabela.tsx · commit 20b0dbf · validado no dev (:5173, Playwright dennis@wenox.com.br, qa-frames/retest-f212-215.mjs, 8/8 PASS, tarefa QA_ descartável deletada no finally): inline-editei status (override+onMudou→reconcilia), mudei o status por FORA via API (`Em andamento`, simula sheet/n8n), disparei refetch sem reload (edição inline de prioridade → onMudou) → a célula passou a mostrar "Em andamento" (override descartado; se o bug persistisse mostraria o valor inline antigo pinado). `tsc --noEmit` exit 0.

## F-213 | categoria: funcional | severidade: média | status: corrigido
- Tela: src/tarefas/TarefasTabela.tsx:388-399 (célula Prazo, `<input type="date">`).
- Passos: 1) Ter uma tarefa cujo `prazo` inclua hora (ex.: "2026-06-25 14:30" — `temHoraPrazo`/`prazoBR` suportam hora, format.ts:44-74). 2) Clicar na célula Prazo dessa tarefa (abre o editor inline). 3) Clicar fora SEM alterar nada (ou trocar só o dia).
- Esperado: (a) clicar e sair sem mudar não deveria disparar PATCH; (b) editar a data não deveria APAGAR o componente de hora já salvo.
- Observado: dois defeitos no `onBlur`: (1) `defaultValue={(te.prazo ?? '').slice(0,10)}` já trunca a hora, e `onBlur` grava `salvarInline(t.id, { prazo: e.target.value })` com o valor `"YYYY-MM-DD"` do input date — ou seja, qualquer passagem pela célula REESCREVE o prazo sem a hora (perda de dado silenciosa: "2026-06-25 14:30" → "2026-06-25"). (2) O `onBlur` dispara o save INCONDICIONALMENTE, mesmo quando o valor não mudou → PATCH desnecessário a cada clique-e-sai, e (combinado com 1) basta abrir e fechar a célula pra zerar a hora. Correção mínima: só salvar se `e.target.value !== (te.prazo ?? '').slice(0,10)`, e preservar a hora original ao remontar a string de prazo.
- (resolvedor) Correção: no `onBlur` da célula Prazo (a) só salva se `novoDia !== diaAtual` (compara o dia do input com `(te.prazo).slice(0,10)`) → focar+sair sem mudar é no-op, zero PATCH; (b) novo helper `combinarPrazoComHora(novoDia, prazoOriginal)` troca só o DIA e reanexa o componente de hora original (`prazoOriginal.slice(10)` quando `temHoraPrazo`) → a hora salva sobrevive à edição da data; dia limpo grava `''` (remove o prazo). Esc continua cancelando sem salvar: como `setEditCell(null)` no Esc desmonta o input e dispara `onBlur`, adicionei `prazoCancelRef` (idioma do `confirmado` em CellEditor) que faz o `onBlur` pular o save. · src/tarefas/TarefasTabela.tsx · commit bba2f61 · validado no dev (:5173, Playwright dennis, qa-frames/retest-f212-215.mjs, 8/8 PASS, tarefa QA_ com prazo `2026-09-15 14:30` deletada no finally): abrir a célula e sair sem mudar → `patches=0`; mudar o dia p/ 2026-09-16 → PATCH com `prazo="2026-09-16 14:30:00.000Z"` (hora 14:30 preservada, dia novo). `tsc --noEmit` exit 0.

## F-214 | categoria: funcional | severidade: média | status: corrigido
- Tela: src/tarefas/TarefasTabela.tsx:485-487 (split abertas/concluídas) + :528 (opacity) + select inline :368-376 · depende de format.ts:19-23 (`tarefaConcluida`).
- Passos: 1) Na tabela, abrir o editor de Status de uma tarefa. 2) O `<select>` lista TODAS as opções de TODOS os grupos via `getGrupos()`/`opcoesDoGrupo()` — incluindo as do grupo "Concluído" cujo nome NÃO é "Concluído" (ex.: "Agendado", "Postado", ou qualquer opção conclusiva customizada criada por um gestor, ex.: "Entregue"). 3) Escolher uma dessas (a gravação usa `espelhoStatus`, que seta `status` = nome).
- Esperado: opção pertencente ao último grupo (categoria "feito", `opcaoEhConclusiva` = true no modelo novo) deveria mover a tarefa para a seção "Tarefas concluídas" e aplicar o estilo concluído (opacity), coerente com o modelo de GRUPOS.
- Observado: o split (`abertas`/`concluidas`, :486-487), o `opacity-60` (:528) e o `catPrazo` (:140) decidem conclusão por `tarefaConcluida(statusEfetivo(t))`, que é NAME-based (format.ts:19-23: só conta como concluída se o nome === "Concluído" OU contém "conclu"). Isso DIVERGE do modelo de grupos usado pelo editor inline: uma opção conclusiva cujo nome não casa com "conclu" (Agendado/Postado/custom) é gravada com sucesso mas a tarefa PERMANECE na seção "em aberto", sem risco/opacity, e ainda pode aparecer como "vencida" no prazo. Inconsistência name-based × group-based. Correção: o split deve usar `opcaoEhConclusiva(te.status_opcao)` (ou `grupoDaOpcao`) em vez do `tarefaConcluida(nome)` legado. (Acessório: o select oferece opções exclusivas de post — Agendado/Postado — para tarefas, o que torna o cenário acionável na UI.)
- (resolvedor) Correção: fonte canônica do modelo de grupos confirmada em status.ts:227 (`opcaoEhConclusiva(opcaoId)` = a opção pertence ao ÚLTIMO grupo "feito"). Novo helper `tarefaConcluidaGrupo(t)`: resolve a opção via `resolverOpcao(t.status_opcao, t.status)` e retorna `opcaoEhConclusiva(op.id)`; fallback name-based (`tarefaConcluida`) só p/ legado sem opção resolvível. Apliquei nos 3 pontos name-based: split `abertas`/`concluidas` (via `concluidaEfetiva` que aplica o override local antes), `opacity-60` da linha, e `catPrazo` (uma opção conclusiva deixa de ser marcada como "vencida"). `resolverOpcao` dá precedência ao nome, e o espelho gravado (`espelhoStatus`) seta status_opcao+status juntos → opções "Agendado"/"Postado"/custom do grupo Concluído passam a casar. · src/tarefas/TarefasTabela.tsx · commit 310ac28 · validado no dev (:5173, Playwright dennis, qa-frames/retest-f212-215.mjs, 8/8 PASS, tarefa QA_ deletada no finally): escolhi "Agendado" (op_agendado, grupo Concluído, nome ≠ "conclu") no select inline → a linha saiu da seção em aberto e apareceu sob "Tarefas concluídas" com `opacity-60` (`opacityRows=1`). `tsc --noEmit` exit 0.

## F-215 | categoria: usabilidade | severidade: baixa | status: corrigido
- Tela: src/tarefas/TarefasTabela.tsx:268-274 (`salvarInline` catch) + :283 e :286 (`salvarCelula` catch).
- Passos: 1) Editar Status/Prazo/Prioridade/Descrição inline, ou adicionar comentário pela célula, sob falha (rede caída / 403 / validação do backend).
- Esperado: feedback ao usuário de que o save falhou (mensagem inline em `text-destructive`, padrão já adotado no codebase — ver F-205/ConexaoCard; não há toast).
- Observado: todos os caminhos engolem o erro sem avisar. (a) `salvarInline` `catch` reverte o override otimista silenciosamente (:269-273) — a célula "pisca" de volta ao valor antigo sem explicação, o usuário pode achar que clicou errado. (b) `salvarCelula` para descrição faz `catch { /* */ }` (:283) mas MANTÉM o `descOverride` setado em :282 → a descrição aparece salva na tela mas NÃO foi persistida (mentira visual em erro). (c) comentário idem `catch { /* */ }` (:286) — falha silenciosa, sem `setComentado`, mas também sem aviso. Correção: estado de erro por célula exibido inline; no caso (b), reverter o `descOverride` no catch (mesma lógica do `salvarInline`).
- (resolvedor) Correção: novo estado `erroSalvar: Record<`${id}:${campo}`, string>` exibido inline no `<td>` em `text-destructive` (mensagem `ERRO_SALVAR`, idioma de F-205/ConexaoCard — sem toast). (a) `salvarInline` ganhou param `campo?: ColKey`; no catch, além de reverter o override, seta o erro daquela célula (status/prazo/prioridade); (b) `salvarCelula` de descrição agora REVERTE o `descOverride` no catch (era mantido → mentira visual) e seta o erro; (c) comentário seta o erro no catch. A mensagem é limpa no início da próxima tentativa (`limparErro`). · src/tarefas/TarefasTabela.tsx · commit 59cb5e6 · validado no dev (:5173, Playwright dennis, qa-frames/retest-f212-215.mjs, 8/8 PASS, tarefa QA_ deletada no finally): com `route.abort()` no PATCH de tarefas, editei a prioridade inline (Média→Baixa) → apareceu "Não foi possível salvar. Tente de novo." na célula e a prioridade reverteu p/ "Média" (override descartado, sem mentira visual). `tsc --noEmit` exit 0.

## F-007 | categoria: usabilidade | severidade: baixa | status: corrigido
- Tela: /dashboard (PRODUÇÃO app.wenox.com.br, logado dennis@wenox.com.br) · evidência: screenshot ss_7323rxpre (Dashboard completo) + zoom dos widgets "Etapas Pendentes" e "Estágio das Tarefas".
- Passos: 1) Login → Dashboard. 2) Rolar até a 2ª metade do dashboard. 3) Observar os widgets "Etapas Pendentes", "Validação Pendente" e "Estágio das Tarefas".
- Esperado (dado o deliverable "Remoção das Etapas do fluxo" — tarefas viraram entidades simples, sem fluxo sequencial): o dashboard não deveria mais apresentar widgets cuja semântica é de ETAPAS de fluxo, ou esses widgets deveriam ser renomeados/repropósitados para "Tarefas".
- Observado: o dashboard ainda exibe 3 blocos herdados do modelo de etapas, agora populados com TAREFAS SIMPLES: (1) **"Etapas Pendentes"** lista as tarefas planas QA_TESTE_inline_F007 e "teste" rotuladas como "Tarefa · Dennis Fernandes" — ou seja, são tarefas, não etapas, sob um título "Etapas Pendentes" (inconsistência semântica / rótulo enganoso); (2) **"Validação Pendente"** mostra "Nenhuma etapa aguardando validação do cliente." — texto ainda fala em "etapa"; (3) **"Estágio das Tarefas"** lista as mesmas tarefas com badge de status ("Não iniciado"). Não há erro de runtime (a tela carrega normal), mas os rótulos "Etapas"/"Estágio"/"validação de etapa" são resíduos do fluxo removido e podem confundir. NÃO testei o lado do código (sou o navegador-visão). Pergunta objetiva de PRODUTO: **os widgets "Etapas Pendentes" / "Validação Pendente" / "Estágio das Tarefas" do dashboard devem ser removidos, renomeados ("Tarefas Pendentes"/"Estágio das Tarefas"→ok) ou re-vinculados ao Quadro de Posts (onde as etapas de fato passaram a viver)?** Registrado como `precisa-decisão` porque pode ser intencional manter como visão de status de tarefas — mas o título "Etapas" diverge do novo modelo. Fora do fluxo /tarefas em si (que está 100% correto), porém diretamente ligado ao deliverable de remoção de etapas.
- (resolvedor) DECISÃO DO USUÁRIO: REMOVER os 3 widgets (coerente com o MVP sem etapas). Correção: causa-raiz = blocos herdados do modelo de etapas (PR #45 removeu o fluxo, mas o Dashboard ainda os montava). Fix mínimo: (1) DashboardPage.tsx — removidos `<EtapasPendentesBloco/>`, `<ValidacaoPendenteBloco/>`, `<EstagioTarefasBloco/>` do layout (2 grids + 1 bloco) e do import; (2) blocosNegocio.tsx — removidas as 3 funções de componente + helpers órfãos que só as alimentavam (`dataCor`, `contextoTarefa`) + imports que ficaram sem uso (useState, ListChecks, ArrowRight, prazoLimite, prazoBR, StatusOpcaoChip, aguardandoAprovacaoCliente, prazoEfetivo, Tarefa type, dataBR, EtapasStepper, TarefaSheet). FONTE COMPARTILHADA preservada: `useDadosAgencia` e os blocos que ficam (KPIs, Desempenho da Equipe, Pulso da Equipe) intactos; nenhuma derivação de etapa de outros widgets apagada (PulsoEquipeBloco segue usando temEtapas/etapaAtual/prazoVencidoEfetivo). Sem buraco de layout: removidos grids/bloco inteiros, sem coluna órfã. Não toquei backend nem outras telas. · Arquivos: src/dashboard/DashboardPage.tsx, src/dashboard/blocosNegocio.tsx · commit 3591fa6 (branch fix/dashboard-remover-widgets-etapas) · validado no dev (Vite :5188, logado dennis@wenox.com.br): /dashboard renderiza sem os 3 widgets (get_page_text termina em "Pulso da Equipe"; nenhum texto "Etapas Pendentes"/"Validação Pendente"/"Estágio das Tarefas"); KPIs (Clientes 67, Projetos 48, Tarefas abertas 1, atrasadas 0), Desempenho da Equipe, Tendência de pontualidade, Desempenho por membro e Pulso da Equipe íntegros; ZERO erro no console (load + navegação); layout sem buracos. `tsc --noEmit` exit 0. → corrigido.

## F-008 | categoria: funcional | severidade: baixa | status: verificado
- Tela: PRODUÇÃO app.wenox.com.br (logado dennis@wenox.com.br) · /tarefas + sheet de tarefa + /minha-area · evidência: ss_5763egatp (form Nova tarefa), ss_46477gt5z (seletor Colunas /tarefas), ss_84544qhw8 (seletor Colunas /minha-area), ss_7037ak603 (sheet sem Etapas), ss_8421e3t9v (/minha-area Lista).
- Passos (validação da entrega "Remoção das Etapas do fluxo — MVP estilo Notion"): 1) /tarefas → "Nova tarefa" (botão header E "+ Nova tarefa" no rodapé — ambos abrem o MESMO sheet). 2) Inspecionar o form de criação. 3) Abrir o sheet de uma tarefa (clicar no nome). 4) Abrir seletor de Colunas em /tarefas e em /minha-area. 5) Carregar /minha-area aba "Lista".
- Esperado: tarefas viram entidades simples e independentes — sem seção "Etapas do fluxo" no form/sheet, sem colunas de etapa ("Etapa atual"/"Status da etapa"/"Progresso") no seletor, responsáveis múltiplos sem restrição, e /minha-area Lista só com colunas básicas.
- Observado: TUDO CONFORME ESPERADO (sem defeito). (a) O form "Nova tarefa" (header E rodapé idênticos) tem só: Nome, Status, Prioridade, Repetir, Projeto, Cliente, Responsáveis, Etiquetas, Descrição — ZERO seção "Etapas do fluxo". (b) Responsáveis é multi-select com checkboxes, selecionei 3 (Dennis + Davi Azevedo + Leonardo Falcão) sem nenhuma restrição → "3 selecionado(s)" + 3 chips; persistiram no sheet após reload. (c) Tarefa criada (QA_F008_teste_inline) aparece na tabela imediatamente. (d) O sheet de edição também NÃO tem seção de etapas. (e) Seletor de Colunas em /tarefas E em /minha-area lista só Cliente/Projeto/Tarefa/Status/Prazo/Prioridade/Responsáveis/Descrição/Comentário — NENHUMA coluna "Etapa atual"/"Status da etapa"/"Progresso". (f) /minha-area aba "Lista" carrega sem erro, só colunas básicas; ZERO erro/exceção no console (onlyErrors) no load e na navegação. Registrado como `verificado` (sem defeito) para documentar a cobertura de teste da entrega de remoção de etapas. NB: não foi possível abrir uma tarefa LEGADA com dados de etapa pois a lista ativa só tem 2 tarefas (busca por "Calendário" → "Nenhuma tarefa"; tarefas antigas com etapas estão arquivadas/migradas) — item "tarefa antiga abre sem erro" coberto indiretamente: a tarefa pré-existente "teste" abre o sheet sem erro e sem resíduo de etapa.

## F-009 | categoria: funcional | severidade: baixa | status: verificado
- Tela: PRODUÇÃO app.wenox.com.br (logado dennis@wenox.com.br) · /tarefas tabela Notion · evidência: ss_7843t8icb (status inline Em produção), ss_3317f4kdm (Concluído → seção "Tarefas concluídas"), ss_8568ynnpl (date picker inline), ss_988089ooj (3 pills de Prioridade), ss_4696i7sne (filtro Status), zoom pós-reload (Concluído/28-06/Alta persistidos).
- Passos (validação da entrega "Tabela estilo Notion — edição inline"): 1) Habilitar colunas Status/Prazo/Prioridade no seletor de Colunas. 2) Clicar direto na célula Status → trocar valor. 3) Mudar Status para "Concluído". 4) Clicar na célula Prazo → alterar data (Enter) e cancelar (Esc). 5) Clicar na célula Prioridade → escolher uma pill. 6) Reload para conferir persistência. 7) Clicar no NOME da tarefa. 8) Aplicar filtro de Status no topo.
- Esperado: clicar nas células de Status/Prazo/Prioridade edita inline (sem abrir o sheet); Concluído move para "Tarefas concluídas"; Enter/blur salva o prazo e Esc cancela; pill salva e fecha; clicar no nome ABRE o sheet; filtro de Status funciona.
- Observado: TUDO CONFORME ESPERADO (sem defeito). (a) Status: a célula vira um `<select>` inline com todos os grupos/opções (Não iniciado…Postado incl. grupo Concluído), trocar salva na hora SEM abrir o sheet (Não iniciado→Em produção); mudar para "Concluído" moveu a tarefa para a seção colapsável "Tarefas concluídas (1)" e baixou a contagem de abertas. (b) Prazo: a célula vira `<input type="date">` inline; digitar 28/06/2026 + Enter salvou (26→28); digitar 15/07/2026 + Esc CANCELOU (reverteu para 28/06) — sem abrir o sheet. (c) Prioridade: a célula mostra 3 pills (Alta/Média/Baixa); clicar "Alta" salvou e colapsou para badge único. (d) Persistência confirmada: após reload completo, QA_F008 = Concluído + 28/06/2026 + Alta (gravou no backend, não só otimista). (e) Clicar no NOME da tarefa abre o sheet lateral (Status/Prioridade/Responsáveis refletindo os valores editados inline). (f) Filtro de Status no topo funciona: "Não iniciado" mostra só a tarefa em aberto e oculta a seção concluídas; reset volta tudo. Registrado como `verificado` (sem defeito) para documentar a cobertura de teste da edição inline. NB: o popup nativo do `<select>` do Chrome não é capturado em screenshot — a troca de status foi feita por teclado (typeahead/setas) sobre o select inline focado, comportamento equivalente ao clique do usuário.

## F-010 | categoria: funcional | severidade: baixa | status: verificado
- Tela: PRODUÇÃO app.wenox.com.br (logado dennis@wenox.com.br) · /tarefas tabela Notion · Sprint 1 "S1 — Edição inline ampliada" · evidência: ss_7795b7juq (nome vira input), ss_8275xufsc (sheet via ícone ⤢ mostra "QA_SPRINT1_A_ED"), ss_17692bl4v (multi-select de responsáveis), ss_4455kq8e5 (3 marcados), ss_3089nth68 (chips de etiqueta), ss_01393tt6w (responsáveis+etiqueta persistidos pós-reload).
- Passos: 1) Clicar na célula do NOME → editar inline, Enter salva, Esc cancela. 2) Hover na linha → ícone "abrir" (⤢) na célula do nome → abre o sheet. 3) Clicar na célula Responsáveis → multi-select com avatares, marcar vários, fechar clicando fora. 4) Ativar coluna "Etiquetas" no menu Colunas → clicar na célula → digitar+Enter adiciona chip, "x" remove. 5) Reload p/ conferir persistência.
- Esperado: nome editável inline (Enter salva/Esc cancela); ícone ⤢ abre sheet; responsáveis multi-select fica aberto p/ seleção múltipla e persiste; etiquetas como chips add/remove e persiste.
- Observado: TUDO CONFORME ESPERADO (sem defeito). (a) Nome: clicar abre `<input>` inline; digitar "_ED" + Enter salvou (confirmado: sheet via ⤢ mostra "QA_SPRINT1_A_ED"); digitar "_XYZ" + Esc CANCELOU (nome permaneceu "_A_ED"). (b) Ícone ⤢ aparece no hover da célula do nome e abre o sheet completo. (c) Responsáveis: clicar abre dropdown com avatares/checkmarks; marquei Davi + Leonardo Falcão (além de Dennis) → 3 avatares na célula (DF/DA/LF); o menu PERMANECE aberto para seleção múltipla e fecha ao clicar fora; persistiu após reload (3 avatares). (d) Etiquetas: coluna "Etiquetas" é nova no menu Colunas; clicar na célula abre input; "urgente"+Enter e "revisao"+Enter criaram 2 chips; clicar no "x" de "urgente" removeu o chip (restou "revisao"); persistiu após reload. Registrado `verificado` (sem defeito) documentando a cobertura de S1.

## F-011 | categoria: funcional | severidade: baixa | status: verificado
- Tela: PRODUÇÃO app.wenox.com.br · /tarefas · Sprint 1 "S2 — Agrupar por" · evidência: ss_54866dihj (agrupado por Prioridade: Alta/Média/Baixa + rodapés), ss_6070je2wc (grupo Alta colapsado), ss_2994r6bbr (agrupado por Status: Não iniciado/Concluído), zoom pós-reload (persistiu "Prioridade"). localStorage: chave `wenox-tarefas-tabela-group-v1` (global) + chaves por área (`wenox-tarefas-Social Media-view-v1`, `wenox-tarefas-Design-tabela-colunas-v2`).
- Passos: 1) Dropdown "Agrupar por" (ícone de camadas) no topo. 2) Escolher Status / Prioridade. 3) Colapsar um grupo. 4) Reload p/ persistência.
- Esperado: opções Sem agrupar/Status/Cliente/Responsável/Prioridade/Projeto; grupos colapsáveis com contador; Status na ordem das opções, Prioridade Alta→Baixa; persiste e é isolado por área.
- Observado: CONFORME ESPERADO (sem defeito). (a) Dropdown tem exatamente as 6 opções do roteiro. (b) Agrupar por Prioridade → grupos "Alta (2)" → "Média (1)" → "Baixa (1)" (ordem Alta→Baixa correta), cada um com contador e RODAPÉ próprio de cálculos (ex.: Alta "2 tarefas · 1 concl · 50% · 2 altas"). (c) Agrupar por Status → "Não iniciado (3)" → "Concluído (1)" (ordem = ordem das opções de status); rodapé por grupo (Concluído "1 concl · 100%"). (d) Grupos colapsáveis: clicar no chevron colapsa/expande (rows somem, badge permanece). (e) Persistência: após reload o agrupamento "Prioridade" permaneceu; localStorage tem chave de group global + chaves por área (confirma "isolado por área", embora não tenha forçado 2 áreas com dados distintos). Registrado `verificado`.

## F-012 | categoria: funcional | severidade: baixa | status: verificado
- Tela: PRODUÇÃO app.wenox.com.br · /tarefas + /minha-area · Sprint 1 "S3 — Ações em massa" (parte que passou) + "S4 — Cálculos de rodapé" · evidência: ss_4200bpw7q (barra "1 selecionada"), ss_9725i248p (2 selecionadas), ss_2435xfhcx (bulk Status→Em andamento aplicado), ss_636261mcp (dropdown Prioridade), bulk Prioridade→Média aplicado, ss_1475l7bos (header checkbox = 3 selecionadas), ss_8616n0323/ss_8797cs6m0 (rodapé S4).
- Passos: 1) Coluna de checkbox à esquerda + checkbox no cabeçalho. 2) Selecionar 1+ → barra flutuante. 3) Bulk Status. 4) Bulk Prioridade. 5) Header checkbox = selecionar todas. 6) Checkbox NÃO abre o sheet. 7) Rodapé com total/%concluído/vencidas/alta (S4).
- Esperado: checkbox seleciona sem abrir sheet; barra "N selecionadas · Status ▾ · Prioridade ▾ · Prazo · Apagar · ✕"; bulk status/prioridade aplicam a todas; header = todas; rodapé com cálculos por tabela/grupo.
- Observado: CONFORME ESPERADO nestes pontos (sem defeito). (a) Coluna de checkbox presente; clicar marca a linha e NÃO abre o sheet; surge a barra flutuante com exatamente os controles do roteiro. (b) Bulk Status: 2 selecionadas → dropdown agrupado (A FAZER/EM ANDAMENTO/CONCLUÍDO) → "Em andamento" aplicou às 2 e limpou a seleção. (c) Bulk Prioridade: dropdown Alta/Média/Baixa → "Média" aplicou às 2. (d) Header checkbox marcou TODAS (3 selecionadas). (e) S4 rodapé: cada tabela/grupo tem linha de rodapé com "N tarefas · X concl · Y% · N vencida(s) · N alta(s)" — números batem (ex.: grupo Concluído 1 concl·100%; lista com tarefa de 10/06 → "1 vencida"). **NB:** o BULK PRAZO falhou (corrupção de ano) — ver [[#F-013]]; o BULK APAGAR usa diálogo nativo bloqueante — ver [[#F-014]]. Estes 2 pontos viram findings próprios; o resto de S3+S4 está `verificado`.

## F-013 | categoria: funcional | severidade: média | status: corrigido
- Correção (commit 58b85e6): adicionada guarda `dataValida()` em `TarefasTabela.tsx` que só grava prazo vazio (limpar) ou ISO com ano de 4 dígitos plausível (>= 1000), aplicada no prazo inline (onBlur) e no prazo em massa (onChange). Valores parciais (ex.: `0002-08-20`) são rejeitados, eliminando a corrupção do ano. Em produção após deploy.
- Tela: PRODUÇÃO app.wenox.com.br · /tarefas · editor de PRAZO (célula inline E barra de ações em massa) · evidência (backend, via API): após digitar "20/08/2026" o registro gravou `prazo: "0002-08-20"` (exibido como 20/08/1902) em QA_SPRINT1_B, QA_SPRINT1_A_ED e QA_SPRINT1_DEL1; já QA_SPRINT1_DEL2 gravou corretamente `2026-08-20`. Screens: ss_2060b1ku9 (3 linhas viram 20/08/1902), zoom do editor inline mostrando o input "resetar" para dia "02" no meio da digitação do ano.
- Passos: 1) Selecionar tarefa(s) e abrir o editor de Prazo (inline na célula OU input "dd/mm/aaaa" da barra de ações em massa). 2) Digitar uma data com ano de 4 dígitos, ex.: "20082026" (20/08/2026) ou por segmentos "20","08","2026", + Enter.
- Esperado: gravar a data digitada (ex.: 2026-08-20).
- Observado: o ano é truncado para os primeiros dígitos → grava `0002-08-20` (ano 2 → exibido 1902 pela conversão JS de ano de 2 dígitos). Dia/mês corretos, ano corrompido. Confirmado no BACKEND (não é só visual): 3 de 4 escritas gravaram `0002`. Reproduzido tanto no editor INLINE da célula quanto no PRAZO em MASSA. Visualmente, durante a digitação do ano a célula RE-RENDERIZA e o `<input type=date>` reseta (o dia volta a "02"), indicando que o editor commita a data num `onChange` intermediário (quando o ano ainda tem 1 dígito → "0002") e fecha/re-renderiza antes do ano de 4 dígitos completar, perdendo o resto da digitação. Efeito: prazos viram 1902, tarefas aparecem como "vencidas" em massa (rodapé acusou "vencidas" indevidas). É INTERMITENTE (QA_SPRINT1_DEL2 gravou 2026 certo) — a janela de commit depende do timing. Correção típica: commitar o prazo só no `blur`/`Enter`/seleção COMPLETA (não em cada onChange parcial), e não re-renderizar/desmontar o input no meio da digitação. **Ressalva de ambiente:** esta sessão teve travamentos do renderer (timeouts de CDP) que podem agravar o timing; recomenda-se re-teste limpo, inclusive via o seletor de calendário (picker), que não consegui acionar por screenshot. Mesmo assim, a corrupção persistente no BACKEND com digitação pausada (0,4s entre segmentos) indica defeito real no commit, não só artefato de automação.

## F-014 | categoria: usabilidade | severidade: média | status: corrigido
- Correção (commit 58b85e6): o "Apagar em massa" não usa mais `window.confirm()` nativo. A barra flutuante agora entra em modo de confirmação in-app ("Apagar N tarefas? Cancelar / Apagar"), não-bloqueante e capturável por automação. Em produção após deploy. **Pendente de re-teste controlado:** o escopo do delete vs. seleção (observação não-verificável da rodada anterior por causa dos freezes) — agora testável sem o diálogo nativo travar.
- Tela: PRODUÇÃO app.wenox.com.br · /tarefas · barra de ações em massa → botão "Apagar" · evidência: ao clicar "Apagar" o renderer congelou de forma reprodutível (CDP `Input.dispatchMouseEvent`/`Page.captureScreenshot` em timeout de 30–45s, `executeScript` bloqueado), sintoma clássico de `window.confirm()` nativo bloqueando a thread; após o usuário clicar OK no diálogo do Chrome, a página voltou e o backend confirmou a exclusão (QA_SPRINT1_DEL1/DEL2 removidos, status 204 equivalente).
- Passos: 1) Selecionar 1+ tarefas (descartáveis). 2) Clicar "Apagar" na barra flutuante.
- Esperado: pedir confirmação e remover em lote (idealmente via modal in-app, não-bloqueante e capturável).
- Observado: o "Apagar" em massa **pede confirmação** (atende o roteiro) MAS aparentemente via `window.confirm()` NATIVO do navegador — que BLOQUEIA a thread do renderer (congelou toda a automação/eventos da página até o diálogo ser dispensado manualmente). Após confirmar (OK), a exclusão em lote FUNCIONA (tarefas removidas, verificado no backend). Recomendação: trocar o `window.confirm` por um modal de confirmação in-app (padrão do app), consistente com o resto da UI e não-bloqueante. **Ressalva (não verificável nesta rodada):** após DOIS ciclos de congela→OK intercalados, sobraram apagadas mais tarefas do que as 2 que selecionei (também A_ED, B e a pré-existente "teste"). NÃO consigo afirmar com limpeza se o bulk-delete excluiu ALÉM da seleção (provável efeito dos cliques/seleções desencontrados durante os freezes) — fica como **observação para re-teste controlado** do escopo do delete vs. seleção, não como bug confirmado. (Obs.: recriei a tarefa "teste" via API; histórico antigo não recuperável.)

## F-015 | categoria: usabilidade | severidade: baixa | status: fechado (by-design)
- Decisão (dev): comportamento PRETENDIDO, não é regressão. A seção "Atrasadas" na `TarefasTabela` só existe quando há um MÊS específico selecionado no filtro — ali "atrasada" = tarefa com competência (mês do prazo) ANTERIOR ao mês escolhido, mostrada à parte para não poluir a visão do mês. Sem filtro de mês ("Mês: todos"), não há recorte de mês e portanto não há seção "Atrasadas" — as vencidas aparecem na lista com a data em vermelho + contagem "N vencida(s)" no rodapé. O roteiro de regressão assumiu incorretamente que a seção apareceria sempre. Para reproduzir a seção: filtrar por um mês (ex.: "Mês atual") com tarefas de meses anteriores em aberto.
- Tela: PRODUÇÃO app.wenox.com.br · /tarefas (Sem agrupar) e /minha-area (Lista e Card) · evidência: ss_50587tc4b (/tarefas: tarefa vencida 10/06 em vermelho, rodapé "1 vencida", SEM seção "Atrasadas"), ss_0251yvzko (/minha-area Lista idem), ss_6441jd20d (/minha-area Card: vencida só no donut "Atrasada 50%").
- Passos: 1) Ter ≥1 tarefa com prazo no passado (setei QA_SPRINT1_D p/ 10/06/2026 via API; hoje=26/06/2026). 2) Abrir /tarefas no modo "Sem agrupar".
- Esperado (item do roteiro de regressão): seções "Atrasadas" E "Tarefas concluídas" aparecem no modo "Sem agrupar".
- Observado: "Tarefas concluídas" aparece como seção colapsável (✓). Porém NÃO há uma seção distinta "Atrasadas" — as tarefas vencidas ficam na lista principal com a DATA em vermelho + contagem "N vencida" no rodapé (e, no Card de /minha-area, no donut "Atrasada %"). Não encontrei o cabeçalho/seção "Atrasadas" em nenhuma das views. `precisa-decisão`: **deveria existir uma seção dedicada "Atrasadas" no modo Lista/Sem agrupar (possível regressão), ou a indicação atual (data vermelha + rodapé "vencidas") é o comportamento pretendido?** Registrado como pergunta de produto pois não há prova de que a seção "Atrasadas" existisse antes nesta tabela (pode ser conceito só da visão Card).
